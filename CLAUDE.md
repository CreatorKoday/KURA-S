# CLAUDE.md

このファイルは、このリポジトリで作業する Claude Code(および開発者)向けのガイドです。

## プロジェクト概要

**KURA:S** ― 家庭内の在庫(食材・日用品)を管理するモバイル向けWebアプリ(PWA的なSPA)。
AI写真判定・手動入力で在庫を登録/消費でき、在庫が少なくなると買い物リストに自動連携する。

- 認証・DB: Supabase
- AI商品判定: Google Gemini API(`gemini-3.1-flash-lite`)
- ビルドツールなし。素のHTML/CSS/JS(ESモジュール)のみで構成された静的サイト

## 開発ルール

### 基本方針

- **保守性・拡張性を最優先**する。奇抜な最適化やトリッキーな書き方より、誰が読んでも分かるコードを優先する
- **既存機能・既存UIを壊さない**。動作確認が難しい変更は、影響範囲を明示したうえで慎重に進める
- **必要最小限のファイルのみ編集**する。関係のないファイルには触れない
- **変更範囲を最小限にする**。ついでの修正・ついでの整形はしない
- **リファクタリング時は見た目・動作を一切変更しない**。見た目や挙動が変わる場合はリファクタリングと呼ばない
- **実装前に必ず設計案を提示**し、私の承認後に実装する
- **大規模な変更は小さなステップに分けて**進める。1つのステップごとに動作確認できる単位にする
- **実装後は必ずローカルで動作確認**し、結果を報告する

### 編集プロセス(必ず守ること)

1. **実装前に、変更方針・変更対象ファイル・実行予定コマンドを含む設計案を提示する**
2. **私(ユーザー)の承認を得る**
3. **承認後は、設計案の範囲内であれば、個々の編集・コマンド実行のたびに確認を求めず、まとめて進める**
4. **不要なリネームを行わない**(ファイル名・変数名・関数名など)
5. **不要なフォーマット変更を行わない**(インデント統一、改行整形、クォート統一など、指示されていない整形はしない)

設計案の範囲を超える変更が必要になった場合(対象外ファイルの編集、方針と異なるアプローチへの変更など)は、あらためて確認を取る。

### データベース

- データベース構造を変更する場合は、必ず `sql` フォルダにSQLファイルを作成する
- SQLは連番付きで管理する

```
sql/
  001_create_product_master.sql
  002_create_product_name_alias.sql
  003_add_product_master_id_to_items.sql
  004_add_product_master_rls.sql
  005_add_product_master_update_policies.sql
  006_add_product_master_icon_and_edited_fields.sql
  007_create_item_lots.sql
  008_drop_barcode_column_from_items.sql
  009_drop_tag_column_from_items.sql
  010_drop_quantity_and_expiry_date_from_items.sql
  011_add_product_master_hiragana_readings.sql
  012_add_low_stock_threshold_to_product_master.sql
  013_add_product_master_id_to_shopping_list.sql
  014_add_price_to_item_lots.sql
  015_create_item_history.sql
  016_add_item_type_to_item_history.sql
  017_create_households_and_members.sql
  018_add_household_id_to_inventory_tables.sql
  019_add_actor_nickname_to_item_history.sql
  020_replace_room_name_with_email.sql
  021_use_verified_email_for_forgot_key.sql
  022_create_household_pending_via_magic_link.sql
  023_allow_household_members_delete.sql
  024_create_household_product_settings.sql
  025_add_nickname_reuse_and_rename.sql
  026_add_regenerate_room_key.sql
  027_add_update_household_email.sql
```

- SQLだけをチャットへ貼り付けるのではなく、必ずSQLファイルも作成する
- Supabaseで実行が必要な場合は、「Supabaseでこのsqlを実行してください」と案内する

### Git

- 機能単位でコミットできるように実装する
- 作業終了時に推奨コミットメッセージを提案する

### AI実装

- AI呼び出し回数を最小限にする
- キャッシュを最優先で利用する
- 商品マスタを優先利用する
- 将来の拡張性を考慮した設計にする
- 商品マスタ未ヒット時にAIへ標準商品名を判定させる際は、既存の標準商品名・検索キーワードとの文字列類似度(bigram重なり係数、`js/productMaster.js` の `fetchKnownCanonicalNameCandidates`)でローカルに絞り込んだ候補をヒントとして渡し、表記ゆれ(例:「豚小間切れ」⇔「豚小間切れ肉」)で同じ標準商品名を再利用しやすくする。この絞り込みはAIを呼ばずに行うため、AI呼び出し回数は増えない
- 使用モデルは `gemini-3.1-flash-lite` に固定している(2026-07-16)。無料枠の1日あたりリクエスト上限が少ない `gemini-flash-latest`(Gemini 3.5 Flash系、無料枠20回/日)で上限超過エラーが頻発したため切替。別モデルへの自動フォールバックは実装しない方針
- 標準商品名・サブカテゴリー・検索キーワードのひらがな読みは、商品属性生成の同じAI呼び出し(`identifyProductAttributes`)にまとめて含めており、専用の追加呼び出しは行わない。カテゴリーはenumが固定のためAIを使わず`js/productMaster.js`の固定表(`getCategoryReading`)で読みを持つ
- 商品マスタ解決(`resolveProductMaster`)は、入力文字列の正規化結果(`normalizeProductName`)が`product_name_alias`(過去に入力した生の商品名のキャッシュ)に無くても、続けて`product_master.canonical_normalized_name`(既存の標準商品名そのもの)と完全一致するかを確認し、一致すればAIを呼ばずその商品マスタを再利用する(該当raw_nameは以後のためにaliasへもキャッシュする)。一致するのは文字列が完全に同じ場合のみで、bigram類似度のような曖昧な一致は使わない(`forceRegenerate: true`のときはこのチェックもスキップし必ずAIを呼ぶ)
- Gemini呼び出し(`identifyProductAttributes`・`identifyProductsWithAI`)がレート制限エラー(HTTP 429・`RESOURCE_EXHAUSTED`)を返した場合、`js/utils.js`の`withGeminiRetry()`が1分待って1回だけ自動的に再試行する(`temperature: 0`で結果が決定的なため、短時間に何度も再試行しても無意味。GeminiのRPM(1分あたりのリクエスト数)上限に達した直後は同じ1分間の枠内で再試行してもまた失敗する可能性が高いため、待機は1分単位にしている)。再試行中は共通通知欄に「AIが混み合っています。1分後に自動で再試行します」を表示し(`isGeminiRateLimitError`で判定)、再試行も失敗した場合のみエラーとする。専用のDB管理された回数制限(1日◯回/1時間◯回など)は設けていない(2026-08時点の判断: RPD(1日の上限)には余裕があり、RPMはこの再試行で個別に緩和されるため、追加の制限テーブル・RPCを持つ複雑さに見合うメリットが無いと判断した)。商品属性の再生成ボタン(`js/productDetail.js`の`product-detail-regenerate-btn`)・作成ボタン(`product-detail-create-btn`)は、リクエスト中はボタン自体をdisabledにすることで連打を防ぐ(これで十分なため、専用の回数制限は追加しない)

### 作業終了時

毎回以下を実施すること

- CLAUDE.mdを最新状態へ更新する
- 変更内容を要約する(何を、なぜ変更したか)
- 変更したファイル一覧を表示する
- 動作確認結果を報告する
- 改善案があれば提案するが、指示なく勝手に実装しない
- 次回おすすめの作業を3件提案する

## 日本語対応

- 常に日本語で会話する
- エラーメッセージの解説や改善提案もすべて日本語で行う
- ドキュメント・設計書・コメント・説明文は日本語で作成する
- Gitのコミットメッセージは日本語で提案する
- SQLファイル名・JavaScriptファイル名などのコード資産は英語のままとし、説明は日本語で行う
- 専門用語を使う場合は、初心者にも分かるように簡単な説明を添える

## UI/UX方針

- デザインコンセプトは **「PayPayの使いやすさ」× 「ME:Iの未来感・高級感」**
  - PayPay的: タップ数が少ない、迷わない、目的の操作に最短で辿り着ける導線
  - ME:I的: 上品なパープル基調、繊細な余白とタイポグラフィ、控えめで上質なアニメーション
- **シンプルで、情報量が多くても見やすいUI**にする(在庫一覧・買い物リストなど情報密度が上がりやすい画面で特に意識する)
- **スマホファースト**(`.wrap` の `max-width: 460px` を前提に設計。PC幅対応は二の次)
- **主な利用環境は iPhone 14 相当**(論理解像度 390×844pt、@3x)。UI変更時はこのビューポートで折り返し・見切れが起きないか確認する(横並びの列を増やしすぎない、ボタン内テキストが折り返さない、など)
- **アクセシビリティ**を意識する(`aria-label`、タップ領域の大きさ、コントラスト、フォーカス可視性など)

## コード方針

### CSS(`css/`)
- **画面・コンポーネント単位で管理**する(例: `home.css` はホーム画面、`modal.css` はモーダル共通部品)
- 共通のトークン(色・角丸・影など)は `common.css` の CSS変数(`:root`)にまとめる。新しい色や数値を各ファイルにベタ書きしない
- 新しい画面・コンポーネントを追加する場合は、既存の分割方針に合わせて新規CSSファイルを作り、`index.html` の `<head>` に `<link>` を追加する

### JavaScript(`js/`)
- **機能単位でモジュールを分割する**(例: `consume.js` = 消費関連、`shopping.js` = 買い物リスト関連)。既存の分割粒度に合わせる
- **共通処理は再利用できるようにまとめる**(`utils.js`, `elements.js` などに集約。同じロジックをコピペしない)
- 新規モジュールは `js/main.js` に副作用importとして追加する(既存の初期化パターンに合わせる)
- モジュール間の依存は `export` / `import` を使う(グローバル変数は最小限に)。ただし `onclick="..."` 属性から呼ぶ必要がある関数は `window.xxx` を使う既存パターンを踏襲する
- **コメントは必要最小限**。「何をしているか」ではなく「なぜそうしているか」が非自明な場合のみ書く
- **読みやすいコード**を優先する(早すぎる抽象化・過剰な汎用化はしない)

## セキュリティ上の既知事項(取り扱い注意)

- `js/config.js` に Supabase anon key と **Gemini APIキーが直書き**されている。このリポジトリは Public のため、新しいキーや秘匿情報を追加する際は特に注意する
- バーコードスキャン機能(`html5-qrcode`・OpenFoodFacts照会・`items.barcode`列)は廃止済み(2026-07-16)。関連コード・DB列は削除済み
- タグ機能(`items.tag`・`tags.js`・タグ別フィルター)は廃止済み(2026-07-16)。カテゴリー(`product_master.category`、無ければ`items.category`)・サブカテゴリー(`product_master.sub_category`)による分類へ統一済み
- 認証はメール/パスワードを廃止し、Supabaseの匿名サインイン+ルームキーによる「部屋(household)」単位に置き換え済み(2026-08-01)。`items`/`item_lots`/`shopping_list`/`item_history`は`household_id`でRLS分離されており、異なる部屋のデータは互いに見えない(`sql/017`〜`019`、詳細はデータモデルの項目を参照)。`product_master`/`product_name_alias`は方針通り部屋をまたいで共有するマスタのまま
- ルームキーは`create_household_pending`関数が発行する10文字のランダム文字列で、キーを知っている人なら誰でもその部屋に入室できる(招待制ではない)。部屋を退室する機能(`js/auth.js`のログアウトボタン)、ルームキーを再発行する機能(`regenerate_room_key`、`sql/026`。アカウント画面の「キーを再発行する」ボタン)はいずれも実装済み(詳細はデータモデルの項目を参照)
- 部屋の新規作成・「キーを忘れた場合」はどちらもSupabaseのマジックリンク(`signInWithOtp`)でメールアドレスの所有権を確認する方式に統一済み(2026-08-03)。**確認コード(`verifyOtp`)方式は廃止した**: Supabaseは2026-06-03以降、無料プランのデフォルトメール機能ではAuthメールテンプレートのカスタマイズができず(独自SMTP設定時を除く)、確認コードをメール本文に表示すること自体が困難だったため、テンプレート編集が不要なマジックリンク方式(ボタン/リンクは`{{ .ConfirmationURL }}`でありテンプレート未編集でも標準で機能する)に統一した
- マジックリンクを機能させるには、Supabaseダッシュボードの Authentication → URL Configuration で、Site URL・Redirect URLsに実際にこのアプリをホストしている(またはテストしている)URLを登録しておく必要がある(設定が無いとメール内のリンクを開いても正しく戻ってこられない)
- iPhoneでホーム画面に追加して(standalone表示で)利用している場合、メール内のリンクはホーム画面のアプリ内ではなく通常のSafariで開かれる(ホーム画面に追加しただけのWebアプリはリンクの自動的な登録ができないため)。そのため、マジックリンク確認後のキー表示画面には**コピーボタン**と**「そのまま入室に進む」ボタン**の両方を用意している(`#auth-key-reveal`)。ホーム画面アプリ利用時はコピーボタンでキーをコピーしてホーム画面アプリ側に貼り付け、通常のブラウザタブ利用時は「そのまま入室に進む」でその場から入室フォームへ進める

## データモデル(部屋・認証まわり)

- **`households`** = 部屋そのもの(メールアドレス・ルームキー。部屋名の概念は廃止済み、2026-08-01)。**`household_members`** = 部屋に参加しているユーザー(Supabaseの匿名サインインで発行される`user_id`)とニックネーム(1ユーザーにつき1部屋のみ、`user_id`にunique制約)。部屋の作成・キーでの入室は、SECURITY DEFINER関数`create_household_pending`/`join_household_by_key`(`sql/017`、`create_household_pending`は`sql/022`で新設)経由でのみ行い、両テーブルへの直接のinsert/update/deleteはRLSで許可していない(SELECTのみ可)
- `create_household_pending()`(引数無し、`auth.email()`=マジックリンクで検証済みのメールアドレスを使う)は**`households`の行だけを作り、`household_members`への登録は行わない**(2026-08-03〜)。部屋の作成者も、他のメンバーと全く同じ`join_household_by_key`経由で実際に入室する(作成者だけの特別な経路が無くなり、仕組みがシンプルになっている)。以前の`create_household(text, text)`(メールアドレス・ニックネームを受け取り同時にメンバー登録も行っていた関数)は`sql/022`で削除済み
- 部屋作成時に登録する`households.email`(unique)は、入室画面の「キーを忘れた場合」でルームキーを確認するためだけに使う。Supabaseのマジックリンク(`signInWithOtp`)でそのメールアドレスの所有権を確認できた場合だけルームキーを表示する`find_room_key_for_verified_email`(`sql/021`、引数を取らず`auth.email()`=現在のセッションのJWTに含まれる検証済みメールアドレスで検索。なりすまし対策)。マジックリンクのリンクを開くとセッションが匿名から実メールアドレスの認証済みセッションに置き換わるため、`js/auth.js`の`handleVerifiedEmailReturn()`が(部屋の新規作成・キーを忘れた場合のどちらであっても)確認後に`signOut`→`signInAnonymously`で通常の入室待ち状態に戻す。新規作成かキーを忘れた場合かは、マジックリンクの`emailRedirectTo`に付与したクエリパラメータ`auth_intent`(`create`/`forgot_key`)で判別する
- `households.email`はアカウント画面(`js/account.js`)から確認・変更もできる(2026-08-10〜)。変更は新規作成・キーを忘れた場合と同じくマジックリンクでの所有権確認を経由し、確認後に`update_household_email(p_room_key text)`(`sql/027`、SECURITY DEFINER。`join_household_by_key`と同じくルームキーを鍵に対象の部屋を特定する)を呼んで反映する。対象を特定するルームキーは`localStorage`ではなくマジックリンクのURL自体(`emailRedirectTo`のクエリパラメータ`room_key`)で渡す(ホーム画面に追加してアプリ化(PWA)している場合、メール内のリンクはアプリ内ではなくSafari側で開かれ、SafariとPWAは`localStorage`を共有しないため、`localStorage`経由だと更新処理そのものが失敗してしまうため。詳細は次の項目を参照)。マジックリンク確認直後のセッションは元々部屋に入室していた匿名セッションとは別物(`auth.uid()`が変わる)になるため、送信前に`js/account.js`が今の匿名セッションの認証情報(アクセストークン・リフレッシュトークン)だけを`localStorage`(`kurasEmailChangeSession`)へ保存しておき、確認完了後に`js/auth.js`の`handleEmailChangeReturn()`が`update_household_email`呼び出し(これはURLから渡されたルームキーだけで完結するため、この時点で更新自体は既に成功している)の後、保存しておいた認証情報で`supabaseClient.auth.setSession()`により元のセッションへの復元を試みる。これはあくまで「同じブラウザでリンクを開いた場合に、そのブラウザでも引き続き部屋に入室した状態にする」ためのおまけの利便性向上であり、復元できなくても更新自体は成功している。PWA利用時などで復元できない場合(Safari側の`localStorage`には`kurasEmailChangeSession`が無いため)は、その画面(Safari側)だけ通常の入室待ち状態に戻すフォールバックになる(ホーム画面に追加したアプリ側のセッションはこの処理で一切触れていないため、そのまま入室状態を保つ)
- ログイン中のユーザーがどの部屋に属するかは`current_household_id()`関数(SECURITY DEFINER、`household_members`をRLSを介さず検索する)で解決し、`items`/`item_lots`/`shopping_list`/`item_history`の`household_id`列のデフォルト値・RLSポリシーの両方でこれを使う(`sql/018`)。そのためアプリ側のコード(`js/items.js`・`js/shopping.js`等)は`household_id`を意識せず、これまで通りinsert/selectするだけで自動的に部屋ごとに分離される
- 同様に`current_nickname()`関数が、ログイン中のユーザーのニックネームを`item_history.actor_nickname`列のデフォルト値として自動設定する(`sql/019`)。「誰が操作したか」を後から履歴に表示する機能は未実装(2026-08-01時点ではデータの記録のみ)
- 部屋名を後から変更する機能は無い。部屋を退室する(`js/auth.js`のログアウトボタン)と、この端末ではルームキーを再入力しない限り同じ部屋に戻れない。**「退室」は確認ダイアログの後、`household_members`の自分の行を削除してから`signOut`する**(2026-08-03〜。以前はセッションを破棄するだけで自分の行が残り続け、「参加中メンバー」にいつまでも表示されてしまっていた)。`item_history.actor_nickname`は`household_members`への外部キー参照ではなく記録時点のニックネームをコピーしただけの単なるテキスト列のため、退室(`household_members`の行削除)は過去の購入・消費履歴の表示に影響しない
- ルームキーは`regenerate_room_key()`(`sql/026`、SECURITY DEFINER)でいつでも再発行できる(アカウント画面の「キーを再発行する」ボタン)。`create_household_pending`と同じ方式でランダムな10文字のキーを生成し`households.room_key`を更新するだけで、既存の`household_members`(参加中メンバー)には一切影響しない。キー漏洩時や、メンバーを削除した後に古いキーでの再入室を防ぎたい場合に使う(キーを知っているだけの人は、再発行後は新しい部屋に入室できなくなる)
- `household_members`は元々SELECTのみ許可していたが、アカウント画面からのメンバー削除のためDELETEポリシーを追加済み(`sql/023`、2026-08-03)。オーナー/管理者の区別は無いため、同じ部屋のメンバーなら誰でも他のメンバーを削除できる(既存のSELECTポリシーと同じ考え方)。自分のニックネームを変更できるよう、自分の行(`user_id = auth.uid()`)のみ更新可能なUPDATEポリシーも追加済み(`sql/025`、2026-08-03。アカウント画面`js/account.js`から利用)
- `join_household_by_key(p_room_key, p_nickname)`は、入室先の部屋に入力したニックネームと完全一致する既存メンバーがいる場合、新規メンバーとして追加せず、その既存メンバーの`user_id`を今回のセッションのものに置き換える(`sql/025`、2026-08-03。同じ人が別端末・再入室などで入室し直した場合に、メンバーが重複しないようにするための「同じ人としての引き継ぎ入室」)。一致しなければ従来通り新規追加する。**この仕組みは「同じニックネームを入力した人」を無条件に同一人物とみなすため**、別の人が誤って/偶然同じ名前で入室すると既存メンバーの入室情報が上書きされ、元の端末は次回操作時に再入室が必要になる(この誤操作を防ぐため、`js/auth.js`は候補一覧と完全一致するニックネームで送信する前に確認ダイアログを挟む)。この重複防止・引き継ぎロジックは入室時のみのものであり、アカウント画面での明示的なリネーム(後述)は別ロジックで、既存の他メンバーと同じ名前への変更は逆に拒否する(意図しない乗っ取りを避けるため)
- `get_household_member_nicknames(p_room_key)`(`sql/025`)は、まだ入室していない(=`household_members`に行が無い)状態でも、ルームキーを知っていればそのキーの部屋の既存メンバーのニックネーム一覧を取得できるRPC。入室画面(`js/auth.js`)でニックネーム候補表示に使う。入室後はどのみち全メンバー名を見られるため、入室前に名前一覧だけ見える状態が増えても実質的な情報範囲は変わらない

## データモデル(在庫まわり)

在庫は3層に分かれている。

- **`items`** = 商品(名前・分類・単位・`product_master_id`)。数量・賞味期限は持たない(`quantity`/`expiry_date`列は`item_lots`への移行完了により削除済み)。`low_stock_threshold`列は残っているが、商品マスタが無いフォールバック商品(単独カード)でのみ使う(商品マスタがある商品は`product_master.low_stock_threshold`を使うため、この列は使われない)
- **`item_lots`** = 在庫ロット(数量・賞味期限・購入日・価格)。1商品(`items`)に対して複数持てる。**「同じ商品 かつ 同じ賞味期限」のときだけ既存ロットに数量を加算し、賞味期限が異なれば必ず新規ロットになる**。ロットの取得・表示・消費順は常に `expiry_date ASC NULLS LAST`(賞味期限が近い順、未設定は最後)。購入日(`purchase_date`)は在庫確認画面には出さず、各商品名の「ⓘ」インフォメーションシート(画面構成10)でのみ確認できる。価格(`price`、nullable)は手動登録画面でのみ入力でき(食費・日用品費の計算確認が目的)、**常に「1つ当たりの単価」(定量系は100gなど基準量あたりの単価)を保存する**(合計金額で入力した場合も登録時に単価へ変換する。消費が進むと`quantity`が減っていくため、単価を固定しておかないと後から消費した分の金額を計算できないため)。既存ロットへ数量を加算する際は新しい価格の入力が無ければ元の単価を保持する
- **`product_master`** / **`product_name_alias`** = 商品属性(標準商品名・カテゴリー・サブカテゴリー・保存場所・用途・検索キーワードなど、AIが生成)。**すべての部屋(household)をまたいで共有される**マスタで、在庫のライフサイクル(登録・消費・削除)とは独立している(2026-08〜、部屋機能の追加後も共有マスタとしての位置付けは変わらない)。標準商品名(`canonical_name`)・サブカテゴリー(`sub_category`)・検索キーワード(`search_keywords`)にはひらがな読み列(`canonical_name_reading`・`sub_category_reading`・`search_keywords_reading`)があり、AI属性生成時に同じ呼び出しでまとめて生成される(カテゴリーのひらがな読みは固定enumのため、AIを使わず`js/productMaster.js`の`getCategoryReading`の固定表で持つ)。**部屋をまたいで共有されるため、これらの属性を手動で自由編集する機能は廃止済み**(2026-08〜。1つの部屋の誤った編集が他の部屋にまで影響してしまうため)。修正が必要な場合は商品マスタ 詳細/編集シートの「再生成する」ボタン(`regenerateProductMasterAttributes`)でAIに再判定させる(既存の行を上書きし、`edited_fields`は空にリセットする)のみで、標準商品名・カテゴリー・サブカテゴリー・保管場所・用途・検索キーワードいずれも個別のテキスト編集はできない。`icon`・`low_stock_threshold`は`household_product_settings`(下記)へ分離済みのため、この2列は`product_master`には存在しない。ひらがな読み列(`sql/011`)追加前に作成された行など、読み方が`null`のまま残っている既存の商品マスタに、新規登録時のAI判定結果(`resolveProductMaster`)が`canonical_normalized_name`で合流した場合、`js/productMaster.js`の`resolveProductMaster`はその場でAIが生成済みの読み方(`canonicalNameReading`/`subCategoryReading`/`searchKeywordsReading`、追加のAI呼び出しは発生しない)で不足分だけを`UPDATE`し補完する(標準商品名・カテゴリーなど読み方以外の既存の値は上書きしない)
- **`household_product_settings`** = 部屋ごとに独立させたい商品マスタ由来の設定(アイコン`icon`・最低数量`low_stock_threshold`)を持つテーブル(`household_id`+`product_master_id`の組でunique、`sql/024`、2026-08〜)。同じ標準商品名(同じ`product_master_id`)でも部屋によって見た目や最低数量を別々に設定できる(片方の部屋が最低数量を変更しても他の部屋には影響しない)。行が無い(その部屋がまだ一度も設定していない)場合は、アイコンはカテゴリー既定の絵文字、最低数量は0として扱う(`js/productMaster.js`の`resolveItemIcon`、`js/productDetail.js`の各`thresholdCtx`初期化)。商品マスタの`icon`・`low_stock_threshold`列は、この分離にともない削除済み(以前はここに直接書き込んでいたため、部屋を分けても全部屋で共有されてしまっていた)
- **`item_history`** = 購入・消費の追記専用ログ(ホーム画面の「購入履歴」ページで表示)。`item_lots`は消費で数量が0になると行ごと削除され、購入時も既存ロットへ数量を加算する場合があるため、あくまで「今の在庫状態」であって履歴ではない。数量が増減するたびに`js/items.js`の`logItemHistory()`が1行追記する(日付`occurred_at`・商品名`item_name`・標準商品名`canonical_name`・種別(食品/日用品)`item_type`・区分`event_type`(purchase/consumption)・数量・単位・価格・操作者のニックネーム`actor_nickname`(DB側の`current_nickname()`が自動設定、画面への表示は未実装)は、いずれも変更時点のスナップショット。`item_type`は`js/items.js`の`effectiveType()`と同じ判定=商品マスタがあればその`type`、無ければ`items.category`)。`item_id`/`product_master_id`は`on delete set null`にしており、商品や商品マスタを削除しても履歴の行自体は残る(スナップショット列で読める)

在庫確認画面は、`items.product_master_id` が同じ商品を「標準商品名」(`product_master.canonical_name`)の1カードにまとめ、その下に実際の商品名(`items.name`)ごとの区画を表示する(商品マスタが無い商品は、従来どおり単一カードで表示するフォールバックになる)。カテゴリー見出しは `product_master.category`(無ければ`items.category`)、サブカテゴリー見出しは `product_master.sub_category`(無ければ「未分類」)を使う。

数量の増減・削除は必ず `item_lots.id` を主キーに操作する(ロットの数量が0になったらそのロットだけ削除し、他のロット・他の商品には影響させない)。買い物リストとの連動(在庫少なめによる自動追加/自動削除)は `js/shopping.js` の `syncShoppingListForItem()` に一本化されており、在庫の増減・消費・登録などすべての経路がこの関数を経由する。ロットの数量変更経路(`js/items.js`の`resolveLot()`・`persistLotQty()`)は、いずれも変更のたびに`logItemHistory()`で`item_history`へ増減分を記録する。`persistLotQty(lotId, itemId, newQty, previousQty)`は、呼び出し元(画面に表示中の値)から渡された`previousQty`との差分で増えれば購入・減れば消費として記録する(DBへの再取得に頼ると、その読み取りが何らかの理由で失敗した場合に増減方向を誤りかねないため、呼び出し元が既に把握している値をそのまま使う設計にしている)。

**在庫確認画面のロット数量は表示専用**(2026-08-16〜、`[-][+]`ボタン・タップでのピッカーは廃止)。賞味期限がある商品を在庫画面から探して数量だけ足すという操作自体が実用上少なく(日用品以外は消費期限があるため、増やす=買ってきた=登録画面から新規ロットとして登録する方が早い)、間違えて数量を動かしてしまう事故を防ぐ狙い。登録時の入力ミスの修正は、商品名区画のⓘパネル(購入日・削除)の「修正」ボタンから行う(`js/productDetail.js`、画面構成10参照)。この「修正」は`persistLotQty()`とは別の`correctLotQuantity()`(`js/items.js`)を使い、**新しい購入・消費履歴を作らない**(見た目上は登録時のミスを無かったことにする操作のため)。その代わり、購入履歴(`item_history`)から「商品・区分=購入・修正前の数量・購入日」がすべて一致する行を検索し、ちょうど1件だけ見つかった場合に限りその行の数量も書き換える(0件/複数件なら履歴には触れず、その旨を通知するだけにする。一部消費済みのロットは購入履歴の記録(元の購入数量)と修正前の現在値が一致せず0件ヒットになるため、これは「安全側に倒すための意図的な制約」であり、登録直後の未消費ロットのみ購入履歴も連動して直せる)。消費画面(`js/consume.js`)のロット調整UI(`[-][+]`・1/4/半分/すべてボタン・ピッカー)はこの変更の対象外でそのまま残っている。

同じⓘパネルには賞味期限を直す「期限」ボタン(2026-08-16〜)もある。`item_history`は賞味期限を記録していないため、こちらは履歴側の照合は不要で`item_lots.expiry_date`を直接更新するだけでよい(`js/calendar.js`の`.date-display`クラス自動バインドの仕組みを再利用するため、非表示のプロキシ入力欄`#pd-lot-expiry-edit-proxy`をクリックしてカレンダーを呼び出す)。修正後の日付が同じ商品の別ロットの賞味期限とたまたま一致しても自動では合算しない(登録ミス修正という限定用途のため、頻度の低いこのケースは対応せず許容している)。

商品名(`items.name`)も同じシートの商品名見出し横の✏️ボタンから修正できる(2026-08-16〜、item/fallbackモードのみ。masterモードは商品名を持たないため非表示)。あくまで誤字修正程度の用途を想定した簡易な機能で、`items.name`を直接`UPDATE`するだけ。**商品名は新規登録のたびに完全一致で既存商品を検索するキー(`resolveItem`)として使われているため、修正すると以下の影響がありうることを把握した上で導入している**: (1)他のメンバーが修正前の名前で登録すると別商品として重複登録されてしまう、(2)過去の購入・消費履歴の商品名(`item_history.item_name`)は記録時点のスナップショットのため古い名前のまま残る、(3)買い物リストの重複判定も商品名の完全一致のため、修正前後で同じ商品として扱われなくなる可能性がある。誤字修正という限定用途であればこれらは許容範囲と判断した。

- **商品マスタがある商品**: 最低数量は標準商品名(カード)単位。同じ`product_master_id`を共有する**すべての商品名の在庫合計**と`product_master.low_stock_threshold`を比較する(`syncShoppingListForMaster()`)。判定式は`low_stock_threshold > 0 && combinedQuantity < low_stock_threshold`。個数系(個・パックなど)と定量系(g/ml/kg/L)の商品名が混在する場合、`js/quantity.js`の`computeCombinedStockQuantity()`が「定量系の合計÷定量系の商品数=平均値を個数系の数量に掛けて定量換算してから合算する」変換を行う(例: 200g,400g,2パック → 平均300g×2パック=600g換算、合計1200g)。定量系が1つも無ければ換算せずそのまま合算する。買い物リストへは標準商品名(`canonical_name`)で1件だけ追加し(`shopping_list.product_master_id`で重複判定)、複数の商品名が同時に低在庫になっても項目は増えない
- **商品マスタが無い商品(フォールバック)**: 従来通り、この商品(`items`)単独の在庫合計と`items.low_stock_threshold`を比較する(判定式は同じ`> 0 && < threshold`)。買い物リストへは`items.name`で追加する

最低数量が`0`の場合は、在庫が`0`でも自動追加しない(「管理しない」という意思表示として扱う、という運用上の意図は共通)。

新規商品登録時の `items.category` は、ユーザーが手入力するのではなく、商品マスタ解決(`resolveProductMaster`)が判定した `product_master.type`(食品/日用品)を自動的に反映する(`js/items.js` の `resolveItem`)。商品マスタの判定に失敗した場合のみ `product_master_id` は `null` のままとなり、登録画面に表示される「種別」フォールバック入力欄の値がそのまま `items.category` に入る。

## プロジェクト構成

```
kateikanri-app-Public/
├── index.html          唯一のHTML(SPA)。全ビュー・全モーダルがここに存在
├── css/
│   ├── common.css        デザインシステム基盤(CSS変数・共通カード/ボタン/フォーム/ヘッダー/下部ナビ)
│   ├── animation.css     共通アニメーション
│   ├── modal.css         モーダル共通
│   ├── accordion.css     アコーディオン/その場差し替えパネル共通
│   ├── home.css          ホーム画面(挨拶・お知らせ・今後の機能)
│   ├── register.css      商品登録オーバーレイ・手動登録フォーム(単位チップ・商品名予測変換など)
│   ├── consume.css       消費(削除)系パネル
│   ├── inventory.css     在庫確認画面
│   ├── shopping.css      買い物リスト画面
│   ├── calendar.css      自作カレンダー(日付選択)
│   ├── productDetail.css 商品マスタ 詳細/編集シート・トースト
│   ├── quantityPicker.css 4桁ドラムロール数量ピッカー(登録・在庫・消費で共通利用)
│   ├── history.css       購入・消費履歴ページの表(横スクロール対応)
│   ├── summary.css       購入・消費サマリー(月間カレンダー)ページ
│   ├── balanceSheet.css  在庫収支表(貸借対照表風)ページ
│   ├── navRadial.css     ナビバー中央の円形メニュー(登録・消費)
│   └── account.css       アカウント画面(ルームキー確認・参加中メンバーの確認/削除)
└── js/
    ├── main.js           エントリーポイント。各モジュールを副作用importで初期化
    ├── config.js         Supabase / Gemini 接続設定
    ├── elements.js        共有DOM要素の集約
    ├── utils.js           共通ユーティリティ(メッセージ表示・画面上部の共通通知欄・HTMLエスケープ等)。Gemini呼び出しのレート制限エラー判定(`isGeminiRateLimitError`)・1分待って1回だけ自動再試行する`withGeminiRetry(fn, onWaiting)`もここに集約し、`js/aiPhoto.js`・`js/productMaster.js`から共通で呼び出す。日付(YYYY-MM-DD文字列)を「今年ならm/d、それ以外の年ならyyyy/m/d」に整形する`formatMonthDay`(2026-08-16〜)もここに集約し、在庫確認画面の期限・購入日(`js/items.js`の`formatExpiryLabel`・`js/productDetail.js`の`formatPurchaseDateLabel`)、ホーム画面の賞味期限のお知らせ・使い切り目安の期限(`js/home.js`の`formatNoticeExpiry`)から共通で呼び出す
    ├── quantity.js        単位から個数系/定量系(g・ml等)を判定するユーティリティ・AI判定レビューカードの数量欄(プルダウン⇔テンキー)切替。標準商品名(カード)単位の最低数量判定用に、個数系・定量系混在時の合算値(`computeCombinedStockQuantity`)と代表単位(`representativeUnitForEntries`)もここに集約(`js/shopping.js`・`js/items.js`から共通で呼び出す)
    ├── quantityPicker.js  4桁ドラムロール数量ピッカー(他モジュールに依存しない独立部品。登録・在庫・消費から共通で呼び出す)
    ├── units.js           商品名から単位を自動推定
    ├── items.js           商品(items)・在庫ロット(item_lots)のCRUD・一覧描画。商品登録オーバーレイ(`#register-overlay`)の開閉制御(AI写真判定・手動登録・買い物リスト「購入済み」からの登録、すべてが共通で使う)も持ち、手動登録の商品名欄には既存商品名・標準商品名(ひらがな読み込み)を候補表示する予測変換がある(候補選択直後だけ「詳細を入力する」を表示し、選ぶと入力欄末尾に`,`を追記できる。登録時は`,`より前だけを商品マスタ解決の検索キーに使い、保存名は`前(後)`の形に組み立てる。`parseNameWithDetail`)。この検索キーが既存の商品名・標準商品名と完全一致すると、過去の平均単価(整数)を価格欄へ自動入力しモードも「1つ当たり」側に切り替える(`loadPriceAverages`/`findPriceSuggestion`/`applyPricePrefill`。手動編集後は上書きしない)。価格欄には「1つ当たり/合計金額」(定量系は「100◯当たり/購入金額」)のモード切替トグルがあり、登録時に`item_lots.price`が常に単価になるよう変換する(`computeStoredUnitPrice`)。ロットの数量変更経路(`resolveLot`・`persistLotQty`)は、変更のたびに`logItemHistory()`で購入・消費履歴(`item_history`、画面構成12)へ1行追記する。在庫確認画面のロット数量は表示専用(2026-08-16〜、調整ボタン廃止)で、登録ミスの修正は`correctLotQuantity()`(`js/productDetail.js`のⓘパネル「修正」ボタンから呼ぶ)が担う。こちらは新しい履歴行を作らず、条件が揃えば既存の購入履歴行を書き換える(詳細はデータモデルの項目を参照)。在庫確認画面の種別/カテゴリー/サブカテゴリー絞り込みオーバーレイ(`#inventory-filter-overlay`、複数選択・Amazon風2ペイン。`inventoryFilterDefs`配列で種類を定義)と、それに掛け合わせる商品名検索欄(`js/productMaster.js`の`computeItemSearchScore`を利用)、および並び替えオーバーレイ(`#inventory-sort-overlay`。カテゴリ/サブカテゴリ/カード/カード内の4階層を独立設定でき、`INVENTORY_SORT_LEVELS`/`INVENTORY_SORT_CRITERIA`で定義。既定値は`localStorage`に保存)もここで扱う。標準商品名(カード)の見出し横には商品マスタ詳細シートを開く⚙️ボタン(`data-mode="master"`)、各商品名区画には購入日・削除シートを開くⓘボタン(`data-mode="item"`)を描画する(商品マスタが無い商品は従来通り1つのボタン`data-mode="fallback"`にまとまる)。カード見出しのアイコン(`household_product_settings.icon`優先、無ければ`js/productMaster.js`の`getCategoryIcon`でカテゴリー既定の絵文字)は`js/productMaster.js`の`resolveItemIcon`で解決する(消費画面と共通)。最低数量(標準商品名カード単位)も`product_master`ではなく`household_product_settings.low_stock_threshold`から読む(部屋ごとに独立、`masterGroupCardHtml`)
    ├── shopping.js        買い物リストの表示・削除・在庫連動(自動追加/削除)・手動/AIからの追加(item_idと紐付けず、商品名の重複を防止)。在庫連動は`syncShoppingListForItem()`が入口で、商品マスタがある商品は標準商品名(カード)単位の合算判定(`syncShoppingListForMaster()`。最低数量は`household_product_settings.low_stock_threshold`から取得し、`js/quantity.js`の`computeCombinedStockQuantity`で個数系/定量系混在を換算)へ委譲し、標準商品名で1件だけ買い物リストに追加する(`shopping_list.product_master_id`で重複判定)。商品マスタが無い商品は従来通り`items`単独で判定する
    ├── aiPhoto.js         Gemini APIによる写真からの商品判定(登録用)。判定結果は`js/items.js`が持つ商品登録オーバーレイを開いて表示する。レート制限エラー時は`js/utils.js`の`withGeminiRetry`で1分後に1回だけ自動再試行する
    ├── consume.js         消費処理の共通ロジック。AI写真判定・手動検索のどちらも共通オーバーレイ(`#consume-overlay`)の中で完結し、背景タップで閉じる。手動検索は[商品名]>[標準商品名]>[サブカテゴリー]>[カテゴリー]>[検索キーワード]の優先度スコアリング(`js/productMaster.js`の`computeItemSearchScore`、在庫確認画面の検索と共有)でひらがな読みでも一致させる(入力のたびに約280msデバウンスして検索)。検索結果は在庫確認画面と同じ`item-card`の見た目(アイコン・商品名・在庫・賞味期限。アイコンは`js/productMaster.js`の`resolveItemIcon`で解決)で、ヘッダーをタップするとカード内でロット調整欄+専用の確定ボタンがfadeSlideInアニメーション付きで展開する(他のカードをタップすると閉じて差し替え、同じカードなら閉じるだけのトグル。展開中は枠線を強調)。AI写真判定は別途、画面下部の共通レビューリストにまとめて追加され共通確定ボタンでまとめて確定する。どちらもロットの数量欄は在庫確認画面と同じ見た目・操作で、ロットごとに指定した数量を直接反映する
    ├── productMaster.js   商品マスタ(商品属性)の解決・キャッシュ・AI呼び出し(標準商品名・サブカテゴリー・検索キーワードのひらがな読み生成を含む。カテゴリーのひらがな読みは固定表)。AI呼び出し省略のキャッシュ判定(`resolveProductMaster`)は、過去の生の商品名の完全一致(`product_name_alias`)に加えて、入力文字列が既存の標準商品名(`product_master.canonical_normalized_name`)とそのまま完全一致する場合もAIを呼ばず再利用する(`forceRegenerate`時は両方スキップして必ずAIを呼ぶ)。レート制限エラー時は`js/utils.js`の`withGeminiRetry`で1分後に1回だけ自動再試行する。商品検索の優先度スコアリング(`computeItemSearchScore`、[商品名]>[標準商品名]>[サブカテゴリー]>[カテゴリー]>[検索キーワード]、ひらがな読み対応)もここに集約し、消費画面・在庫確認画面の検索から共通で呼び出す。標準商品名(カード)のアイコンを解決する`resolveItemIcon(item)`(`household_product_settings.icon`優先、無ければ`getCategoryIcon`でカテゴリー既定の絵文字)、商品マスタの属性を丸ごとAIに再判定させて既存行を上書きする`regenerateProductMasterAttributes(masterId, sourceName)`(標準商品名・カテゴリー・サブカテゴリー・保管場所・用途・検索キーワードを更新し`edited_fields`を空にリセット。手動編集廃止後の唯一の修正手段)もここに持つ
    ├── productDetail.js   商品マスタ 詳細/編集シートの表示・作成/再生成。商品属性(標準商品名・カテゴリー・サブカテゴリー・保管場所・用途・検索キーワード)は部屋をまたいで共有されるため手動編集は廃止済み(2026-08〜)で、「再生成する」ボタン(`regenerateProductMasterAttributes`)でのAI再判定のみで修正する。手動で編集できるのはアイコン(`household_product_settings.icon`、部屋ごとに独立)のみ。同じオーバーレイをmaster(標準商品名=カード単位。商品属性+最低数量)/item(商品名単位。購入日+削除)/fallback(商品マスタが無い単独商品。従来通りすべてまとめて表示)の3モードで使い回し、`applySectionVisibility()`でセクションの表示/非表示・見出しを切り替える。最低数量(`thresholdCtx.table`が`"product_master"`の場合)は`household_product_settings`へ`upsert(..., {onConflict:"household_id,product_master_id"})`で保存し、部屋ごとに独立させる(fallbackモードの`items.low_stock_threshold`は従来通り)。再生成ボタン・作成ボタン(`product-detail-create-btn`)はいずれもリクエスト中disabledで連打を防ぐ
    ├── shoppingPurchase.js 買い物リスト「購入済み」ボタンの監視・既存商品照合のみを行い、実際の入力・登録は`js/items.js`の商品登録オーバーレイ(手動登録と同じウィンドウ)をそのまま開いて任せる(専用シートは持たない)
    ├── navigation.js      ビュー切り替え(ホーム/買い物/在庫/アカウント)。在庫確認画面以外へ切り替える際は、商品名検索欄(`#inventory-search`)の入力を毎回リセットする。ホーム画面以外では`.app-header-top`(KURA:Sロゴ+ログアウト行)を隠す(共通通知欄`#app-notice`は隠さない、詳細は画面構成の共通通知欄の項目を参照)
    ├── home.js            ホーム画面: 挨拶・日付表示(登録・消費のトリガーは`js/navRadial.js`に移設済み)と、賞味期限のお知らせ(期限切れ/3日前/1週間以内/1ヶ月以内を横並びのタブ+共通パネルで表示、`loadExpiryNotices`)。`item_lots`を全件取得し、ロット単位で4グループへ振り分ける(1週間/1ヶ月は登録時点の賞味期限の長さがそれぞれ7日/30日を超えるロットのみ対象。3日前はこの対象外ルールを適用せず購入日にかかわらず常に表示、詳細は画面構成2参照)。タブをタップすると共通パネルにその内容を開き(同じタブの再タップで閉じる)、パネル内の商品をタップすると`js/items.js`の`focusInventoryOnItemName`を呼び、在庫確認画面をその商品名で絞り込んで開く。**使い切り目安**(2026-08-16〜)は、めんつゆのような減りにくい商品が期限間際までまとめて残っていることに気づきにくい問題を防ぐための機能で、`loadExpiryNotices`が取得済みの同じロット一覧から追加の問い合わせ無しで集計する(`computePaceNotice`)。登録時点の賞味期限が長い商品(定量系=`js/quantity.js`の`isContinuousUnit`がtrueの単位で90日以上、個数系で180日以上。賞味期限切れのロットは「期限切れ」タブと役割が重複するため対象外)を対象に、「経過日数から見て理想的にはあと何%残っているべきか」(`残り日数÷登録時点の賞味期限日数×100`。実際の消費量とは比較しない時間ベースの目安)を商品名・期限・数量とあわせて表示する。件数バッジ+タップで開閉するパネルという見た目・操作感は賞味期限のお知らせと共通(`js/home.js`内で完結する軽量な実装のため専用パネルは1つのみでタブ分けは無い)。並び順は目安%が低い(=消費が遅れていそうな)順。`loadExpiryNotices`はページを開いた時点で1度だけ実行されるため取得結果が古くなりうるが、登録(AI・手動)・消費が完了した直後にも再実行して最新化する(`watchSuccessMessage`で`#manual-add-message`/`#add-message`/`#consume-message`の完了を監視し、それぞれ`loadExpiryNotices`を呼ぶ。2026-08-16〜、登録直後に使い切り目安へ反映されない不具合の修正)。挨拶・日付は約10秒ごとに「今日は何の日？」+その日の記念日名(`js/anniversaries.js`の`ANNIVERSARIES`)と交互に表示する(`startAnniversaryToggle`。該当日のデータが無い日は何も起きず通常表示のまま)
    ├── anniversaries.js   「今日は何の日」の静的データ(`ANNIVERSARIES`、キーは`月-日`)。Wikipedia「日本の記念日一覧」(CC BY-SA 4.0)を基に2026年7月時点の内容から一度だけ生成した366日分の記念日名一覧で、`js/home.js`から参照する。Yahoo Kids「今日は何の日」ページはCORSヘッダーが無くブラウザから直接取得できず、スクレイピングもToS上避けたいため、ライブ取得はせずこの静的データをアプリに同梱する方式にした(データ自体はWikipedia由来のため、Yahoo Kidsのような1日ごとの詳細な説明文は含まず、記念日名のみ)
    ├── navRadial.js       ナビバー中央の円形メニュー(登録・消費、`#nav-radial-btn`)。PayPayの「支払う」ボタンのようにナビバーからはみ出す円形ボタンで、タップのたびに idle(アイコンのみ)→choose(左半分=登録/右半分=消費)→method(左半分=AI/右半分=手動、選んだ側の色を反映)と中身を差し替えながら展開する状態機械を持つ(`state`/`chosenAction`)。method状態でAI/手動を選ぶと、以前ホーム画面のアコーディオンが呼んでいたのと同じ処理(AIはそれぞれ`#photo-btn`/`#consume-photo-btn`のクリックを起点に`js/aiPhoto.js`/`js/consume.js`が処理、手動は`js/items.js`の`openRegisterManualOverlay`・`js/consume.js`の`openConsumeSearchOverlay`)を呼び出し、円はidleへ戻る。円の外側(`#nav-radial-backdrop`)をタップするとどの段階からでもidleへ戻る。左右分割は、円形にクリップした`#nav-radial-content`の中に2つの`flex:1`要素を並べるだけで実現している(境界線を描画しなくても左右それぞれが半円に見える)
    ├── history.js         購入・消費履歴ページ(`#view-history`)の表示。ホーム画面の「購入履歴」カードから`switchView("history")`で開く(下部ナビには無いため、戻るボタンも自分で持つ)。`item_history`を`occurred_at`降順・直近`HISTORY_LIMIT`件だけ取得し、通常は読み取り専用のHTML表(日付/種別/標準商品名/商品名/区分/数量/価格)として描画する(履歴の記録自体は`js/items.js`の`logItemHistory`が行う)。鉛筆ボタンで編集モードに切り替えると削除ボタンと各セルの手入力欄/セレクトが現れ、その場で`item_history`を修正できる。**このページは履歴表の閲覧・編集専用**で、表の上の「フィルター」ボタンで在庫確認画面と同様のAmazon風2ペイン絞り込み(日付・種別・標準商品名・区分・価格)を開き(`matchesHistoryFilters`としてexport。このフィルターは購入履歴ページの表示にのみ効き、サマリー・収支表には影響しない)、「並び替え」ボタンで日付/区分/金額を独立設定できるシンプルな3段スイッチの並び替えを開く(`compareHistoryRows`。`appliedHistorySort`としてexport)。「拡大表示」ボタン(`#history-expand-btn`)はフルスクリーンの`#history-expand-overlay`を開き、CSSで表全体を90度回転させることで、iPhone Safariが対応していない画面回転ロックの代わりに端末を横向きにすると見やすくなる読み取り専用の拡大表示を提供する(`renderHistoryExpandTable`)。金額(数量×単価。定量系は100◯当たりの単価のため換算が必要)の計算は`computeHistoryRowAmount`としてexportし、`js/summary.js`の月間集計・`js/balanceSheet.js`の月次収支計算もこれを共通で使う
    ├── expenseMenu.js     支出管理メニュー(`#view-expense-menu`)の表示。ホーム画面の「支出管理」カードから`switchView("expense-menu")`で開く(戻るボタンはホームへ戻す)。集計表への入口一覧を`EXPENSE_REPORT_DEFS`配列(`{key, label, desc, icon, onOpen}`)から動的に描画するだけの薄いモジュールで、今後表の種類を増やす場合はこの配列に定義を1つ足し、対応するモジュールに`openXxxPage()`のようなexport関数を用意するだけでよい構造にしている(現在は`js/summary.js`の`openSummaryPage`・`js/balanceSheet.js`の`openBalanceSheetPage`の2件)
    ├── summary.js         購入・消費サマリー(`#view-summary`)の表示。支出管理メニューの「日別カレンダー」から`openSummaryPage()`(export)で開く(戻るボタンは支出管理メニューへ戻す)。月間カレンダー(`js/calendar.js`の日付選択カレンダーとは別モジュール。値の選択ではなく集計の表示が目的なため)を表示し、`item_history`の`quantity×price`を日付ごとに集計して(価格が無い記録は金額計算できないため除外)、購入合計(🛒緑)・消費合計(➖オレンジ)を各日付マスに表示する。前月/次月ボタンに加え、月ラベルのタップでも`js/calendar.js`と同じ操作感の年月プルダウン選択で切り替えられる。「フィルター」ボタン(`#summary-filter-btn`)は購入履歴ページの絞り込みと同じ機能・デザインだが、`appliedHistoryFilters`とは独立した専用の状態(`appliedSummaryFilters`)を持ち、このページの集計にのみ反映する(選択肢の一覧は`loadSummaryFilterSourceRows`で`item_history`から軽量に取得し直す)
    ├── balanceSheet.js    在庫収支表(貸借対照表風、`#view-balance`)の表示。支出管理メニューの「月間集計B/S」から`openBalanceSheetPage()`(export)で開く(戻るボタンは支出管理メニューへ戻す)。サマリーと同じ前月/次月ボタンで表示月を切り替え、`item_history`を食品/日用品ごとに集計して借方(在庫)・貸方(前月繰越/今月購入/今月消費)の2カラム表を描画する。前月繰越は表示月の開始日より前の全履歴を「購入は+・消費は-」として累積した金額(=その月の期首在庫評価額)、今月末在庫(借方)は`前月繰越+今月購入-今月消費`で自動計算するため、借方合計・貸方合計は計算上必ず一致する。今月消費は`▲9,000`のような会計風のマイナス表記(`formatAccountingYen`)にする。フィルターは持たず常に月全体を集計する
    ├── auth.js            部屋の作成・キーでの入室・退室。メール/パスワードでのログインは使わず、Supabaseの匿名サインイン(`signInAnonymously`)を裏側で使う。ページ読み込み時、セッションが無ければ匿名サインインし(失敗時は`showBlockedState()`で全フォームを隠しエラーメッセージのみ表示する。「Anonymous Sign-ins」がSupabase側で無効だとここに該当し、有効化が必要)、`household_members`に自分(`auth.uid()`)の行があるか確認して(`fetchCurrentMembership`)、あれば即ホーム画面へ、無ければ**既定で「キーで入室」の入力画面**(`#auth-card`、`#auth-join-form`)を表示する。キーで入室: ルームキー・ニックネームを入力しRPC `join_household_by_key`を呼ぶ。ルームキー欄の入力(デバウンス)・blurのたびに`get_household_member_nicknames`(`sql/025`)でそのキーの部屋の既存メンバーのニックネーム一覧を取得し(`fetchExistingNicknames`)、ニックネーム欄にフォーカス中は「入室中のユーザー」の見出しで候補として表示する(`renderNicknameSuggestions`、`#join-nickname-suggestions`)。候補をタップするとその名前が入力され、「タップして同じユーザーとして入室」という案内文で、同じ名前を選ぶと新規メンバーではなく既存メンバーの入室を引き継ぐ(`join_household_by_key`側の挙動、sql/025)ことを示す。入力したニックネームが候補と完全一致する状態で入室ボタンを押すと、送信前に確認ダイアログ(`confirm()`)を挟み、同意した場合のみRPCを呼ぶ(別の人が誤って同じ名前で入室し、既存メンバーの入室情報を意図せず上書きしてしまう事故を防ぐため)。入室画面の「新規作成はこちら」を押すと部屋作成画面(`#auth-create-form`)に切り替わる: メールアドレスのみを入力し(ニックネームは後の入室ステップで入力する)`signInWithOtp`でマジックリンクをメール送信(`emailRedirectTo`に`?auth_intent=create`を付与。この時点では部屋は作らない)、送信直後は「確認メールを送信しました」の案内画面(`#auth-mail-sent`)を表示する。「キーを忘れた場合」も同様にメールアドレスを入力すると`signInWithOtp`でマジックリンクを送信する(`?auth_intent=forgot_key`)。実際にそのメール内のリンクを開いてこのアプリに戻ってくると、セッションが匿名から実メールアドレスの認証済みセッションに置き換わる(`is_anonymous: false`)ため、`init()`がこれを検知して`handleVerifiedEmailReturn()`を呼び、URLのクエリパラメータ`auth_intent`で新規作成/キーを忘れた場合を判別した上で、`create_household_pending`(新規作成。部屋作成のみ行い、`household_members`への登録はしない)または`find_room_key_for_verified_email`(クライアントからのメールアドレス指定は受け付けず、検証済みセッションの`auth.email()`だけで検索)のいずれかのRPCを呼び、結果のルームキーだけを共通のキー表示画面(`#auth-key-reveal`、コピーボタン+「そのまま入室に進む」ボタン)に表示する。表示後は`signOut`→`signInAnonymously`で通常の入室待ち状態に戻す(部屋の作成者も、他のメンバーと同じ`join_household_by_key`経由で実際に入室するため特別扱いが無い)。コピーボタンは、ホーム画面に追加したアプリで利用している場合にメールのリンクが通常のSafariで開かれてしまう(セッションの引き継ぎができない)ことへの対策で、キーをコピーしてアプリ側の入室フォームに貼り付けられるようにするため。「そのまま入室に進む」は同じ画面のまま入室フォームにキーを自動入力するボタンで、ホーム画面アプリ化していない通常のブラウザ利用時に使う。`household_id`/ニックネームの分離自体はDB側(RLS・デフォルト値、`sql/017`〜`022`)が担うため、このモジュールは部屋への参加/退室の管理のみ行う。入室が確定して`showLoggedIn()`を呼ぶ際、`js/home.js`の`loadExpiryNotices`も呼び直す(2026-08-16〜。`loadExpiryNotices`はスクリプト読み込み時に1度だけ自動実行されるが、その時点ではまだ入室が完了しておらずデータを取得できないため、新規入室直後は賞味期限のお知らせ・使い切り目安が空欄のままになってしまっていた不具合の修正)。「退室」ボタン(`logout-btn`)は確認ダイアログの後、`household_members`の自分の行を`delete()`してから`signOut`+`location.reload()`する(2026-08-03〜。以前は自分の行を削除せずセッションを破棄するだけだったため、退室後も「参加中メンバー」に残り続けてしまっていた)。`handleEmailChangeReturn(roomKey)`(2026-08-10〜)は、アカウント画面からの「メールアドレスを変更する」で送信したマジックリンクの確認後に呼ばれる(`auth_intent=change_email`。対象のルームキーはマジックリンクのURL自体のクエリパラメータ`room_key`から受け取る。`localStorage`ではなくURLで渡す理由はPWA/Safari間で`localStorage`が共有されないためで、詳細はデータモデルの項目を参照)。`update_household_email`RPC(`sql/027`)で対象の部屋の`households.email`を更新した時点で変更自体は完了しており、その後`js/account.js`が送信前に`localStorage`へ保存しておいた元の匿名セッションの認証情報があれば`supabaseClient.auth.setSession()`で復元を試みる(同じブラウザで完結した場合だけのおまけの利便性向上。復元できない場合はその画面だけ通常の入室待ち状態に戻すフォールバックになる)
    ├── account.js         アカウント画面(`#view-account`)の表示。ナビバーの「アカウント」(在庫の右)から開く、PayPayのアカウント画面を参考にしたレイアウト。`households.room_key`(RLSで自分の部屋のみ閲覧可)を表示し(コピーボタン付き、認証画面のキー表示と共通の見た目`.auth-key-display`/`.auth-key-text`を再利用)、その下に「キーを再発行する」ボタン(`account-regenerate-key-btn`)を持つ(確認ダイアログの後`regenerate_room_key`RPC、`sql/026`を呼び、成功したら表示中のキーを差し替える。既存の`household_members`には影響しない)。`household_members`を取得して参加中の全メンバーのニックネームを一覧表示する。自分自身の行には`(自分)`ラベルを付け、削除ボタンの代わりに✏️編集ボタンを表示する(タップするとその場でテキスト入力+保存/キャンセルに切り替わる。保存時、同じ部屋の他メンバーと同じ名前になっていないかクライアント側でチェックしてから`household_members.nickname`を`UPDATE`する。自分の行のみ更新可能なRLSポリシーは`sql/025`で追加済み。成功するとヘッダーの`#user-nickname`表示も更新する)。他のメンバーの行には削除ボタンがあり、確認ダイアログの後`household_members`から直接削除する(`sql/023`でDELETEポリシーを追加済み。オーナー/管理者の区別は無く、同じ部屋のメンバーなら誰でも他のメンバーを削除できる)。ルームキー表示の下には連絡先メールアドレス(`households.email`)の確認・変更UIもある(2026-08-10〜)。表示は読み取り専用のテキストで、「メールアドレスを変更する」を押すと新しいメールアドレスの入力欄が開き、送信するとマジックリンクを送る(`signInWithOtp`、`emailRedirectTo`に`?auth_intent=change_email`を付与)。対象のルームキーは`emailRedirectTo`のクエリパラメータ(`room_key`)として渡す(`localStorage`ではなくURLで渡す理由はPWA/Safari間で`localStorage`が共有されないため)。送信前に今の匿名セッションの認証情報(アクセス/リフレッシュトークン)だけを`localStorage`(`kurasEmailChangeSession`)へ一時保存しておく(マジックリンク確認後、同じブラウザであればそのセッションへ復元するためのおまけの利便性向上用。詳細は`js/auth.js`の`handleEmailChangeReturn()`の項目を参照)。実際の反映・セッション復元処理自体は`js/auth.js`側で行う
    └── calendar.js        自作カレンダー(他モジュールに依存しない独立部品)。日付を選択・クリアすると対象inputへ`change`イベントを発火する(値の直接代入だけでは発火しないため。購入履歴フィルターの日付範囲入力など、変更を監視したい呼び出し元向け)

sql/                        Supabaseに適用したDBスキーマ変更を連番で記録(実行はSupabase側で行う)
```

## 画面構成

**共通通知欄**(`#app-notice`): 画面上部中央に浮かぶiPhone通知風の固定バナー(`position: fixed`、`.wrap`の外側・`<body>`直下に配置、`index.html`では`#toast`の隣)。登録・消費・買い物リスト追加・商品属性の生成/再生成など、完了時の案内メッセージ(旧: 各パネル内に緑字で表示していたもの)はここに一本化して表示する(小さめの文字・表示後4秒で自動的にスライドアウトして消える)。呼び出しは`js/utils.js`の`showAppNotice(text)`(`show`クラスの付け外しでスライドイン/アウトのtransitionを発火させる。連続で呼ばれた場合も一度クラスを外して再度リフローしてから付け直すことで、毎回アニメーションが再生されるようにしている)。商品登録時は「AIが商品属性を生成しました」/「既存の商品属性を利用しました」を先頭に付け、AIが新規に属性を作ったか既存を再利用したかを区別して表示する(`upsertItemByName`/`resolveItem`が返す`productMasterStatus`で判定)。入力エラーなど失敗時のメッセージは対象外で、従来どおり各パネル内に赤字で表示する。ログイン画面(`#auth-card`)はヘッダーが別(`.brand-header`)のため対象外。`.app-header`(KURA:Sロゴ+ログアウト行)からは完全に独立しているため、商品登録オーバーレイなど`z-index:1000`前後の他のシートを開いている間も、`#app-notice`は`z-index:2000`で常にその手前に表示される(以前は`.app-header`内に置いていたため、`.app-header`の`backdrop-filter`が作る重なりコンテキストに閉じ込められ、オーバーレイの裏に隠れてしまっていた。2026-08-10に固定バナー方式へ変更)。**`.app-header-top`(ロゴ+ログアウト行)はホーム画面以外では非表示にする**(`js/navigation.js`の`switchView`が`view!=="home"`の時に`.hidden`クラスを付け外しする。ログアウトはホーム画面からのみ行える)。`#app-notice`は`.app-header`から独立しているため、ロゴ行の表示/非表示に関わらずどの画面でも同じ見た目で案内メッセージを表示できる。ロゴ行が隠れている間、空の`.app-header`カードが浮いて見えないよう余白・枠線を消す処理(`common.css`の`.app-header:has(.app-header-top.hidden)`)は`#app-notice`の状態とは無関係に`.app-header-top`のみを見て判定する。

1. **認証画面**(`#auth-card`): メール/パスワードでのログインは使わず、Supabaseの匿名サインインを裏側で使う(`js/auth.js`)。**既定では「キーで入室」画面**(`#auth-join-form`、ルームキー+ニックネーム)を表示し、タブ切り替えは無い(2026-08-02、以前の「部屋を作る」/「キーで入室」タブ構成は廃止)。「キーで入室」は`join_household_by_key`RPCで既存の部屋に参加する。同じルームキーで入った人同士は同じ在庫(`items`/`item_lots`/`shopping_list`/`item_history`)を共有し、異なる部屋のデータは互いに見えない(RLSによる分離、データモデルの項目を参照)。
   - **ニックネーム候補**: ルームキー欄に入力すると(デバウンス)、そのキーの部屋に既に入室している既存メンバーのニックネーム一覧を「入室中のユーザー」という見出しでニックネーム欄の下に候補表示する(`get_household_member_nicknames`、`sql/025`)。各候補には「タップして同じユーザーとして入室」という案内文を添え、タップするとその名前が入力される。**同じニックネームで入室すると、新規メンバーとして追加されず既存メンバーの入室(`user_id`)を引き継ぐ**(`join_household_by_key`、`sql/025`。別端末・再入室などで同じ人が入室し直した場合にメンバーが重複しないようにする仕組み)。誤って他人と同じ名前で入室してしまう事故を防ぐため、入力したニックネームが候補と完全一致する状態で「入室する」を押すと確認ダイアログを挟み、同意した場合のみ入室する(キャンセルすれば入室せず入室画面のまま)
   - **新規作成**: 入室画面の「新規作成はこちら」リンクから部屋作成画面(`#auth-create-form`)に切り替わる。メールアドレスのみを入力し(ニックネームは後の入室ステップで入力する)「確認メールを送る」→`signInWithOtp`でSupabaseのマジックリンクをメール送信する(この時点では部屋はまだ作らない)。送信すると「確認メールを送信しました」の案内画面(`#auth-mail-sent`)を表示する。実際にそのメール内のリンクを開いてこのアプリに戻ってくると、メールアドレスの所有権が確認できたことになり、**このタイミングで初めて**`create_household_pending`RPCが呼ばれ部屋が作成される(誤ったメールアドレスで部屋が作られてしまうのを防ぐため)。メールアドレスが既に他の部屋で使われている場合はエラーメッセージを表示する
   - **キーを忘れた場合**: 「キーで入室」画面の「キーを忘れた場合」リンクから、部屋作成時に登録したメールアドレスの入力欄(`#auth-forgot-key-form`)に切り替わる。送信の流れは新規作成と同じ(マジックリンクをメール送信→「確認メールを送信しました」)で、リンクを開くとそのメールアドレスに紐づく既存のルームキーを検索する(`find_room_key_for_verified_email`)
   - **キー表示画面**(`#auth-key-reveal`、新規作成・キーを忘れた場合の両方で共通): マジックリンクの確認が完了すると、ルームキーだけを表示するこの画面に進む(この時点ではまだ入室していない)。**コピーボタン**と**「そのまま入室に進む」ボタン**の両方を用意しており、iPhoneでホーム画面に追加して(standalone表示で)利用している場合はメールのリンクが通常のSafari側で開かれてしまう(ホーム画面アプリとは別のセッションになる)ため、キーをコピーしてホーム画面アプリ側の入室フォームに貼り付けて入室する。通常のブラウザタブで利用している場合は「そのまま入室に進む」でその場からルームキー欄に自動入力された入室フォームに進める。マジックリンクを開いても該当するキーが見つからない(未登録のメールアドレスでの「キーを忘れた場合」など)場合はエラーメッセージを表示し、入室画面に戻る。メールアドレスの所有権をマジックリンクで確認してから処理するため、他人になりすまして部屋を作成したりキーを見たりすることはできない(詳細はセキュリティ上の既知事項を参照)
   - ヘッダーのログアウトボタンは「退室」(確認ダイアログの後、`household_members`の自分の行を削除してからこの端末のセッションを破棄する。再度使うにはルームキーの再入力が必要)
2. **ホーム**(`#view-home`):
   - 挨拶・日付表示: 通常は時間帯の挨拶(おはようございます、など)と日付を表示するが、約10秒ごとに「今日は何の日？」とその日の記念日名(`js/anniversaries.js`のWikipedia由来データ、複数ある日は「・」で連結)に交互に切り替わる(`js/home.js`の`startAnniversaryToggle`)。該当日のデータが無い日は交互表示自体が起きず、通常の挨拶・日付のまま
   - 「商品を登録」「商品を消費」は、ホーム画面のカードではなくナビバー中央の円形メニュー(画面構成16、`js/navRadial.js`)から開く。選ぶとどちらもその場では差し替わらず共通オーバーレイを開く(登録は`#register-overlay`・詳細は画面構成7、消費は`#consume-overlay`・詳細は画面構成8)。中身(AI判定結果・手動フォーム・検索・確定)の詳細はそれぞれの画面構成の項目を参照
   - 登録画面(AI・手動とも)に「分類」の手入力欄は無い。新規商品は商品マスタ(AI)が判定した`type`(食品/日用品)を`items.category`へ自動反映する。判定に失敗した場合のみ、その場に「種別」選択欄(食品/日用品)が表示され、選び直して再度「登録」を押すとAIを再呼び出しせずその値で登録される(個別登録・まとめて登録の両方に対応。まとめて登録では種別入力が必要な商品だけカードが残る)
   - 賞味期限のお知らせ: 「期限切れ」「3日前」「1週間以内」「1ヶ月以内」の4つを横並びのタブで表示する(`js/home.js`の`loadExpiryNotices`)。全ロット(`item_lots`)を対象に、期限切れ(残り日数<0)・3日前(0〜3日)・1週間以内(4〜7日)・1ヶ月以内(8〜30日)へロット単位で振り分ける(賞味期限未設定のロットは対象外。4グループは互いに重複しない)。**「1週間以内」「1ヶ月以内」は、登録(購入)時点ですでにその期間を切っていたロットを対象外にする**(`purchase_date`〜`expiry_date`の日数=登録時点の賞味期限の長さが、それぞれ7日・30日を超えていることが条件。例: 最初から賞味期限が1週間の商品を買った場合、いつまで経っても「1週間以内」のお知らせには出てこない。これは、最初から短い賞味期限だと分かって買った商品を今さら知らせる意味が薄いため)。**「3日前」だけはこの対象外ルールを適用せず、購入日にかかわらず常に表示する**(直前に迫った期限は、最初から短い賞味期限だったかどうかに関わらず確実に知らせるべきという考え方のため)。タブをタップすると、下の共通パネル(`#expiry-notice-panel`)にその内容(商品名・期限(月/日)・数量の一覧、該当0件なら「該当する商品はありません」)が開き、同じタブをもう一度タップすると閉じる(一度に開けるのは1つのみ)。パネル内の商品をタップすると在庫確認画面へ遷移し、その商品名で商品名検索欄(`js/items.js`の`focusInventoryOnItemName`)を絞り込んでその商品のカードだけを表示する(遷移時、既存のフィルター設定は全て解除する)。**在庫確認画面から他の画面(ホームなど)へ切り替えると、商品名検索欄の入力は毎回リセットされる**(`js/navigation.js`の`switchView`。特定の商品名で絞り込んだ状態のまま次に在庫確認画面を開いてしまうのを防ぐため)
   - **使い切り目安**(2026-08-16〜): 賞味期限のお知らせのすぐ下に表示する別枠のお知らせ。めんつゆのような減りにくい商品は、賞味期限が近づいてから「まだこんなに残っていた」と気づきがちなため、登録(購入)時点の賞味期限が長い商品(定量系=g/ml/kg/L`は90日以上、個数系は180日以上。`js/quantity.js`の`isContinuousUnit`で判定)を対象に(`js/quantity.js`の`isContinuousUnit`で判定し、定量系=g/ml/kg/Lは90日以上、個数系は180日以上)、経過日数から見て理想的にはあと何%残っているべきか(`残り日数÷登録時点の賞味期限日数×100`、四捨五入。実際の消費量とは比較しない時間ベースの目安)を表示する。賞味期限切れのロットは「期限切れ」タブと役割が重複するため対象外。件数バッジのボタン(`#pace-notice-tab`)をタップすると下のパネル(`#pace-notice-panel`)に商品名・期限・数量・目安%の一覧が開く(もう一度タップで閉じる)。並び順は目安%が低い(=最も消費が遅れていそうな)順。パネル内の商品をタップすると、賞味期限のお知らせと同じく在庫確認画面へその商品名で絞り込んで遷移する。集計は`loadExpiryNotices`が取得済みの同じロット一覧を使い回すため、追加のDB問い合わせは発生しない
   - 今後追加予定カード(AIレシピ。破線の枠で「近日公開」と表示するプレースホルダー)。「購入履歴」「支出管理」の2枚は実装済みで、実線の枠でタップするとそれぞれ購入・消費履歴ページ(画面構成12)・支出管理メニュー(画面構成13)を開く(「家族共有」プレースホルダーは支出管理の実装にともない廃止・転用した)
3. **買い物リスト**(`#view-shopping`): 買い物項目一覧のみで、画面上からの自由入力欄は無い(項目はホーム画面の「商品を登録」の手動/AIから追加するか、在庫少なめによる自動追加で増える)。「購入済み」ボタンを押すと商品登録オーバーレイ(画面構成7の「買い物リスト「購入済み」からの登録」)を開く(即座には確定しない)。各行には「削除」ボタンがある(編集機能は無い)
4. **在庫確認**(`#view-inventory`): タイトル下に「フィルター」(画面構成5)「並び替え」(画面構成6)ボタンを並べて設置。**既定では在庫が完全に無いカード(グループ内のどの商品名にも1件もロットが無い状態)は一覧に表示しない**(フィルターの「在庫」種類で「在庫なしを表示」をチェックすると表示できる、画面構成5参照)。フィルターで在庫の有無/種別/保管場所/カテゴリー/サブカテゴリーを複数選択でき、その下には商品名検索欄(`#inventory-search`)があり、消費画面と同じ優先度スコアリング(`js/productMaster.js` の `computeItemSearchScore`。商品名>標準商品名>サブカテゴリー>カテゴリー>検索キーワード、ひらがな読み対応)で絞り込む。フィルター・検索は**すべて掛け合わせる**(AND条件。フィルターの各種類内は複数選択でOR条件)。並び替えを何も設定していなければ、並び順・カテゴリー/サブカテゴリー見出しなど既存の表示は変更しない(検索欄は入力のたびに約280msデバウンスして再取得する`loadItems()`の再取得方式)。標準商品名(`product_master.canonical_name`)ごとに1カードへまとめ、その下に実際の商品名(`items.name`)の区画・在庫ロット(賞味期限が近い順)を表示する。カード見出し(標準商品名。商品マスタが無いフォールバック商品は商品名)の左には、消費画面の検索結果と同じ考え方でアイコンを表示する(商品マスタの`icon`を優先し、無ければカテゴリー既定の絵文字。`js/items.js`の`resolveItemIcon`、`js/productMaster.js`の`getCategoryIcon`)。**スクロールせずに見える商品数を増やすため、カード・見出し類の余白やフォントサイズは全体的に控えめにしている**(カテゴリー/サブカテゴリー見出しの余白、カード内側の余白、ロット行の余白など)。賞味期限は「期限:7/20 (あと3日)」のように日付と残り日数を併記する(`js/items.js` の `formatExpiryLabel`。期限切れは「期限:7/20 (期限切れ)」、未設定は「期限未設定」)。**ロットの数量はこの画面では表示専用**(2026-08-16〜、`[-][+]`ボタン・タップでのピッカーは廃止。賞味期限のある商品を在庫画面から探して数量だけ足す場面は少なく、登録画面から新規ロットとして登録する方が早いため。登録ミスの修正はⓘパネルの「修正」ボタンから行う、画面構成10参照)、その分カードの縦幅をコンパクトにしている。カード見出し(標準商品名)の隣の⚙️ボタンから商品マスタ 詳細/編集シート(画面構成10。商品属性+最低数量。最低数量は標準商品名=カード単位)を開ける。各商品名区画のⓘボタンからは、その商品名だけの購入日・削除を扱う別シート(画面構成10)を開ける(商品マスタ未作成の商品は、従来どおり単一カードで表示され、その1つのボタンに商品属性・最低数量・購入日・削除のすべてがまとまる)
5. **在庫確認の絞り込みオーバーレイ**(`#inventory-filter-overlay`): Amazonアプリの商品検索フィルターに近い2ペイン構成。左側1列にフィルターの種類一覧(先頭が「在庫」、続けて「種別」「保管場所」「カテゴリー」「サブカテゴリー」の順に階層になっている。今後「登録日」「購入金額」「消費期限」などを追加する場合は`js/items.js`の`inventoryFilterDefs`配列に定義を足すだけでよい構造にしている)、右側にその種類の選択肢をチェックボックス形式で表示する(複数選択可、選択済みをもう一度タップすると解除)。**「在庫」だけは他の種類と違い、データから選択肢を集計するのではなく固定の1択「在庫なしを表示」を持つだけの特別な種類**(`INVENTORY_STOCK_FILTER_OPTION`)で、種別/保管場所/カテゴリー/サブカテゴリーとのカスケード関係も持たない。既定(未選択)では、グループ(標準商品名カード、または商品マスタが無いフォールバック商品)内のどの商品名にも1件もロットが無い(=在庫が完全に無い)カードを一覧から除外し(`renderItems()`内、`groupItemsByMaster`直後)、チェックすると在庫なしのカードも表示する。種別(`effectiveType()`。商品マスタがあればその`type`、無ければ`items.category`が種別のフォールバック値をそのまま持つ)の選択を変えると対象外になった保管場所・カテゴリーの選択が、カテゴリーの選択を変えると対象外になったサブカテゴリーの選択が、それぞれ自動で外れる(カスケード)。**保管場所**(`effectiveStorage()`。`product_master.storage`のみが持つ属性で、商品マスタが無いフォールバック商品にはフォールバック値が無くこの絞り込みには一致しない)は種別に応じて選択肢が変わる(食品なら常温/冷蔵/冷凍、日用品なら洗面所/キッチン/トイレ/浴室/収納棚/玄関/その他。`js/productMaster.js`の`FOOD_STORAGE_OPTIONS`/`DAILY_STORAGE_OPTIONS`)が、カテゴリー・サブカテゴリーとの間のカスケードは持たない。選択はオーバーレイを開いている間だけの一時状態で、右下の「結果を表示」を押すまでは在庫一覧に反映されない(背景タップ/×で閉じると破棄される)。左下の「フィルターを解除」はその場で選択をクリアするだけでオーバーレイは開いたまま(反映するにはあらためて「結果を表示」を押す)。ボタン群の上には選択中の内容を「[日用品] > [飲料] > [清涼飲料水]」のように案内文で表示する(種別・カテゴリー・サブカテゴリーを" > "で連結、同じ種類内の複数選択は「・」で連結。何も選んでいなければ非表示)。在庫確認画面の「フィルター」ボタンには選択中の合計件数を表示する(例:「フィルター (2)」)
6. **在庫確認の並び替えオーバーレイ**(`#inventory-sort-overlay`): フィルターと同じ2ペイン構成に加え、上部に「すべて」「カテゴリ」「サブカテゴリ」「カード」「カード内」の横並びタブ(`js/items.js`の`INVENTORY_SORT_LEVELS`)があり、この4階層(「カード」=同じサブカテゴリー内でどの商品カードが上に来るか、「カード内」=1枚のカードの中で商品名ごとの区画がどちらが上に来るか)をそれぞれ独立して[期限が近い順/遠い順・登録日が新しい順/古い順・50音順(あ→わ/わ→あ)]から設定できる(`INVENTORY_SORT_CRITERIA`。同じ選択をもう一度タップすると解除)。「すべて」タブ(他のタブと色を変えて区別)だけは実体を持たず、選ぶと4階層すべてに同じ設定を一括反映する特別なタブで、4階層が同じ設定で揃っている場合だけチェック状態や案内文に反映される。何も設定していない階層は、これまで通りの既定の並び順(カテゴリー/サブカテゴリーは文字コード順、カード/カード内は賞味期限が近い順)のまま変わらない。各階層の「代表値」は、その階層の中に含まれる商品・ロットのうち、選んだ並び替えにとって最良のもの(例: 期限が近い順ならもっとも近い期限)を子から親へ伝播させて決める(`js/items.js`の`foldBestInventorySortValue`)。選択は一時状態で、右下の「並び替え」を押すまで反映されない。左下のボタンは「デフォルトに戻す」で、保存済みの既定値(無ければ「並び替えなし」)を選択状態に戻すだけ(パネルは開いたまま)。案内文の隣の「デフォルトに設定」ボタンは、現在選択中の内容(4階層すべて)をブラウザの`localStorage`(`kurasInventorySortDefault`)に保存し、次回以降のアプリ起動時にもこの既定値がそのまま適用される。「登録日」の並び替えは`items.created_at`列(Supabaseの標準的な作成日時列)を利用する(2026-07-20、実データのテーブルに存在することを確認済み)
7. **商品登録オーバーレイ**(`#register-overlay`): AI写真判定・手動登録のどちらもこの共通オーバーレイの中で完結する。商品マスタ 詳細/編集シートと同じく、オーバーレイ背景(シート外側)をタップすると閉じる。
   - **AI写真判定**: 直接カメラ/写真選択を起動し、判定結果が出たらオーバーレイを開いてAI判定結果(`#review-section`)を表示する。各カードには「登録」(その1件だけ登録)「買い物へ」(その1件だけ買い物リストへ追加。在庫ロットは作成・変更せずAIへの再問い合わせもしない)「削除」(AIへの再問い合わせなしで画面から除外)があり、下部の「まとめて登録する」「まとめて買い物リストに追加」はいずれもその時点で画面に残っているカードだけを対象にする。個別・まとめてのいずれかで買い物リストに追加したカードは画面から除外され、以後の「まとめて登録する」の対象にはならない。カードが1件も残らなくなったタイミング(個別処理・まとめて処理・キャンセルのいずれでも)でオーバーレイごと自動的に閉じる
   - **手動登録**: 選ぶと即座にオーバーレイが開き手動フォーム(`#register-manual-section`)を表示する。入力欄の順序は「商品名・単位・数量・消費期限」で、単位・数量は2列の行、消費期限はその下の独立した全幅の行にする(3列に並べるとiPhone 14幅では単位チップ・増減ボタンの文字が折り返してしまうため)。**商品名欄の下には予測変換欄**(`#item-name-suggestions`)があり、入力するたびに「既存の登録済み商品名(`items.name`)」「標準商品名(`product_master.canonical_name`、ひらがな読み`canonical_name_reading`も検索対象)」のうち部分一致するものを最大8件表示する(候補データはオーバーレイを開いた時に一度だけ取得し、以降は入力のたびにクライアント側でフィルタするためAI呼び出し・デバウンスは不要)。予測欄が開いている間は入力欄に`suggestions-open`クラスが付き、入力欄の下端の角丸を消して予測欄と隙間なく接続する(閉じている間は他の入力欄と同じ四隅とも角丸に戻る、Chromeの検索欄と同じ見た目)。並び順は[商品名を優先度上・標準商品名を優先度下]→[各層の中で完全一致>前方一致>部分一致]→[同着は五十音順](商品名欄には基本的に具体的な商品名を入力するため、標準商品名は一致度が高くてもあえて優先度を下げている)。**ただし完全一致(入力欄とまったく同じ文字列)は候補に出さない**(すでに入力欄の内容そのものであり、標準商品名を選んだ直後に予測を表示し続ける仕様と組み合わせると、選んだ本人が候補に残り続けて見えてしまうため)。各候補の先頭には目印として中黒「・」を添える(Google検索の🔍のような機能アイコンではなく、「検索ではなく候補の提示である」ことを示す意味を持たない記号にしている)。標準商品名の候補だけ、テキストの右端に小さく目立たない「標準」バッジを添えて区別する(バッジを左に置くと行ごとに文字の開始位置がずれて読みにくくなるため、あえて右端にしている)。候補をタップすると商品名欄に反映され、通常は予測欄が閉じるが、**標準商品名の候補を選んだ場合だけ予測欄を表示し続ける**(その標準名にちなんだ商品名候補が新たに一致する可能性があるため)。その後に値を変更すると(商品名候補選択後は)再度予測を表示する。**候補(標準商品名・商品名のどちらでも)を選んだ直後だけ**、予測欄に追加で「詳細を入力する」という行を表示する(タップすると商品名欄の末尾に半角カンマ`,`を自動追記し、そのままカーソルを末尾に置く。以後さらに値を変更すると詳細入力欄は消える)。この状態で続けて具体的な内容(例:「小間切れ」)を入力し「豚肉,小間切れ」のようにカンマ区切りで登録すると、**カンマより前だけ**を商品マスタ解決(`resolveProductMaster`)の検索キーに使い(既存の標準商品名・商品名と完全一致すればAIを呼ばない)、実際に保存・表示される商品名(`items.name`)は「豚肉(小間切れ)」のようにカンマの後ろを括弧書きで結合したものになる(`js/items.js`の`parseNameWithDetail`)。カンマを含まない通常の入力では、この変換は行われず従来通り。数量欄は個数系・定量系(g/ml等)を問わず、タップすると4桁ドラムロール(0000〜9999の整数)が下からせり上がって表示され、選択して「完了」で確定する共通ピッカー(`quantityPicker.js`)を使う。数量欄の下には単位チップと同じデザインの`[-][+]`クイック増減ボタンもあり、増減幅は単位が個数系なら1、定量系(g/ml等)なら100(在庫確認画面の`[-][+]`と同じ判定)。ボタンは数量欄と同じ幅で均等2分割・両端揃え(`.qty-quick-adjust-row`)。数量の初期値は単位が個数系なら1、定量系なら100(単位が変わるたびに追従)。ただしピッカーで確定するかクイック増減ボタンを押すと「手動調整済み」扱いになり、以後は単位を変えても自動では上書きしない。単位欄は、商品名から単位候補が1つに確定した場合(例: トマト→「個」)でも、個数・重さ・容量を選び直せるよう常に「個」「g」「ml」チップを表示する(候補が複数ある場合は従来どおりその候補をチップ表示)。**商品名が未入力の段階でも既定で「個」「g」「ml」チップを表示する**(右隣の数量・消費期限欄には常時表示のクイック調整ボタンがあり、単位欄だけ空だと見た目のバランスが崩れるため。この既定表示は`js/units.js`の`updateUnitSuggestions`という手動登録専用のラッパー内で対応しており、単位欄の値そのものは書き換えないため、ユーザーが商品名を入力する前に単位を手入力していてもその値を上書きしない)。価格欄(`#item-price`)と消費・賞味期限欄は2列の行(単位・数量の行と同じ`row2`)にまとめ、価格を左・消費期限を右に配置する。消費・賞味期限欄(ラベルは「消費・賞味期限」。「(任意)」は付けていない)の下には数量と同じデザインの`[-1日][+1日]`クイック調整ボタンがあり、未入力の場合は今日を基準に加減する。

**価格欄**(ラベルは「価格(円・任意)」): 他の数量系入力と違いドラムロールではなく通常の数値キーボード入力(`input type="number"`)にしている(価格は数量と違って決まった刻み幅が無く、大きくばらつく数値になりやすいため)。価格欄の下には、入力した金額の意味を切り替える2択のON/OFFスイッチ風トグル(`#item-price-mode-toggle`。単位チップ・クイック調整ボタンと同じ配色を使い、左右が連結した1つのピルの中で選択中の片側だけ色がつく見た目、数量・期限のボタンのデザインを踏襲)があり、単位が個数系なら[1つ当たり|合計金額]、定量系(g/ml等)なら[100◯当たり(◯は実際の単位)|購入金額]の2択になる(`js/items.js`の`updatePriceModeLabels`。単位欄が変わるたびにラベルも追従する)。**既定(デフォルト)は「合計金額/購入金額」側**(店の値札・レシートは基本的に「今回買った分の金額」を表示することが多く、そのまま入力するだけで済むようにするため)。

実際に`item_lots.price`へ保存する値は、モードに関わらず常に**「1つ当たりの単価」(定量系は100gなど基準量あたりの単価)**に統一する(`js/items.js`の`computeStoredUnitPrice`)。理由: `item_lots.quantity`は消費が進むと減っていく残数のため、合計金額のまま保存すると後から「消費した分の金額」を正しく逆算できなくなる(購入時点の単価を固定しておけば、`消費した数量 × 単価`でいつでも計算できる)。

- 「1つ当たり/100◯当たり」モード: 入力値をそのまま保存する(数量は使わない)
- 「合計金額/購入金額」モード: 個数系は`入力値 ÷ 数量`、定量系は`入力値 × 100 ÷ 数量`を保存する(整数に丸める)

商品名欄の「,」より前(検索キー、`parseNameWithDetail`参照)が既存の商品名または標準商品名と完全一致すると、その平均単価(整数に丸める。商品名が一致すればその商品名だけの平均、無ければ標準商品名が同じすべての商品の平均)を価格欄へ自動入力し、モードも自動で「1つ当たり/100◯当たり」に切り替える(平均値は常に単価のため。一致しなければ価格欄を空にしモードも既定の「合計金額/購入金額」に戻す。`js/items.js`の`loadPriceAverages`/`findPriceSuggestion`/`applyPricePrefill`。オーバーレイを開いた時に一度だけ全ロットの単価を取得しクライアント側で集計するため、AI呼び出し・追加のデバウンスは不要)。価格欄を一度でも手動編集する、またはモードのトグルを一度でもタップすると、以後は自動入力で上書きしない。「登録する」の下には「買い物リストに追加」ボタンもあり、商品名だけを買い物リストへ追加する(在庫ロットは作成・変更しない。単位・数量・期限・価格は使わない)。登録完了後は約700ms後にオーバーレイが自動的に閉じる
   - **買い物リスト「購入済み」からの登録**(`js/shoppingPurchase.js`): 買い物リストの「購入済み」ボタンを押すと、専用のシートではなくこの手動登録と全く同じウィンドウ・入力欄を開く。商品名が既存の`items`と一致する(在庫連動の項目、またはitem_idを持たない項目でも同名の商品が在庫にある)場合は、その商品名・単位(既存の値をそのまま使い、自動推定で上書きしない)・買い物リストで必要だった数量を初期値にする。一致しない場合は商品名・買い物リストの数量だけを初期値にし、単位は手動登録の新規商品と同じ自動提案が働く。消費・賞味期限は常に未入力から始まる。**このモードのときだけ「買い物リストに追加」ボタンを隠す**(買い物リストの項目を消化する場面のため、そこへ追加し直す意味が無い)。商品名の予測変換は通常どおり機能する。登録が完了すると、通常の登録メッセージに加えて対象の買い物リスト行を削除し買い物リストを更新する(種別の自動判定に失敗した場合は、通常の登録と同様に種別を選び直すまで買い物リストの行は削除されない)。キャンセル・背景タップで閉じた場合は買い物リストの行はそのまま残り、次にオーバーレイを開いたときには「買い物リストに追加」ボタンも必ず元通り表示される
8. **消費確認オーバーレイ**(`#consume-overlay`): AI写真判定・手動検索のどちらもこの共通オーバーレイの中で完結する(検索結果が多くなり得るため、専用ページとして分離)。商品マスタ 詳細/編集シートと同じく、オーバーレイ背景(シート外側)をタップすると閉じる。先頭に検索欄(`#consume-search`)があり、入力するたびに約280msデバウンスして検索する(1文字ごとに問い合わせず、かつ入力途中の古いレスポンスは無視するstale-response対策込み)。検索対象・優先度は[商品名]>[標準商品名]>[サブカテゴリー]>[カテゴリー]>[検索キーワード]で、いずれも部分一致(標準商品名・サブカテゴリー・検索キーワードは`product_master`のひらがな読み列、カテゴリーは固定のひらがな対応表でも一致する。商品名自体にひらがな読みは無い)。一致した項目のうち最も優先度が高いものを基準に検索結果を並べる。
   - **手動検索**: 検索結果は在庫確認画面・AI消費レビューと同じ`item-card`の見た目に統一されたカードで、ヘッダー(商品マスタの`icon`または無ければカテゴリー既定の絵文字アイコン・商品名・在庫・もっとも賞味期限が近いロットの賞味期限・開閉シェブロン)をタップすると、同じカードの中でロット調整欄+「確定」ボタンが`fadeSlideIn`アニメーション付きで展開される(他画面の開閉パネルと同じ動き)。展開中のカードは枠線・影を強調しシェブロンが回転するため、どのカードを操作中か一目でわかる。他のカードをタップすると今開いているカードは閉じて新しいカードが展開され、同じカードをもう一度タップするとトグルで閉じる(同時に展開できるのは1件のみ)。この「確定」ボタンは展開中のカード1件だけを対象に消費を確定する(下部の共通確定ボタンとは別物)。確定すると検索語はそのままに検索結果を再取得し、在庫表示・展開状態を作り直す
   - **AI写真判定**: 判定結果に一致した商品は、検索結果のカードとは別に画面下部の共通レビューリスト(`#consume-review-list`)へまとめて追加され(重複追加はしない)、下部の共通「確定」ボタンでリストに残っている全カードをまとめて確定する
   - 両方共通のロットカードの見た目・操作: 在庫確認画面と同じ見た目(手動検索の展開カードのみ商品名ヘッダーは省略。検索結果の行に既に表示されているため)で、賞味期限が異なる同名商品はロットごとに別行になる。**行ごとの数量欄は「消費後にそのロットに残る数量」を表す**(消費する量そのものではない)。初期値はそのロットの在庫数量そのもの(=何もしなければ何も消費しない)。`[-][残り数量 単位][+]`で増減でき(個数系の単位は1ずつ、定量系(g/ml等)は100ずつ、残り数量は0〜元の在庫数量の範囲でクランプ)、数量表示をタップすると4桁ドラムロールピッカーが開き直接入力できる。数量の下の「1/4」「半分」「すべて」ボタンは、押すとその時点で表示されている残り数量から該当する割合を消費した数量にする(例:「1/4」は今の残り数量の1/4を消費し残りをその3/4にする、「半分」は今の残り数量を半分にする、「すべて」は今の残り数量を100%消費して0にする)。押した後の値を基準に次のボタンが働くため、連続で押すと「半分の半分」のように複利的に効く(2026-08-16〜、以前は常に元の在庫数量に対する割合だったため連続で押しても同じ値にしかならなかった)。**個数系(4個入りパックを1個として登録している場合など)で残り数量が1以下になった場合だけ**、この「割合」計算を「1個(パック全体)に対する固定幅の減算」に切り替える(1→0.75→0.5→0.25→0の0.25刻みの階段。通常の割合計算のままだと0.75×0.5=0.375のような中途半端な値になり得るため)。定量系や、個数系でも残り数量が1を超える間はこれまで通り現在値に対する割合で計算する。この0.25刻みの端数は4桁ドラムロールピッカー(`quantityPicker.js`、整数専用)では正しく表示できないため、ピッカーをタップで開いた際に1未満の端数は一律「1」として開く(0は元々整数なのでそのまま)。確定すると、対象カードの各ロットについて残り数量が元の在庫数量から変わっているものだけをその値に更新する(0になったロットは削除、変えていないロットはそのまま)
9. **日付選択モーダル**(`#calendar-overlay`): 自作カレンダー。`.date-display` クラスを持つ input から共通で呼ばれる
10. **商品マスタ 詳細/編集シート**(`#product-detail-overlay`、`js/productDetail.js`): 在庫確認画面の⚙️/ⓘボタンから、対象に応じて3つのモード(`mode`パラメータ)で同じオーバーレイを開く。セクションの表示/非表示・シート見出しはモードごとに切り替わる(`applySectionVisibility()`)。**商品属性(標準商品名・カテゴリー・サブカテゴリー・保管場所・用途・検索キーワード)は部屋をまたいで共有される`product_master`のデータのため、個別のテキスト編集機能は廃止済み**(2026-08〜。誰か1人の誤った編集が他の部屋にまで影響してしまうため)。修正したい場合は見出し横の「再生成する」ボタンでAIに再判定させる(`regenerateProductMasterAttributes`。標準商品名を検索キーとしてAIへ再問い合わせし、既存の商品マスタ行を上書きする。`edited_fields`は空にリセットされる)。手動で編集できるのは**アイコンのみ**(見出し横の「アイコンを編集」ボタン、`household_product_settings.icon`。部屋ごとに独立した設定のため、他の部屋には影響しない)。再生成・アイコン保存・作成のいずれもリクエスト中は対象ボタンをdisabledにし、連打による無駄な呼び出しを防ぐ
    - **masterモード**(カード見出しの⚙️。標準商品名=カード単位): 見出し「商品属性の詳細」。商品属性(標準商品名・種別・カテゴリー・サブカテゴリー・保管場所・用途・検索キーワード、いずれも読み取り専用+再生成ボタン)と、最低数量(`household_product_settings.low_stock_threshold`、部屋ごとに独立)を閲覧・編集できる。購入日(在庫ロット)・削除は表示しない(商品名側のⓘに委ねる)。商品マスタが無いと開けないモードのため「商品属性を作成」の空状態は無い
    - **itemモード**(各商品名区画のⓘ。商品マスタがある商品のみ): 見出し「購入日・削除」。その商品名(`items.id`)の在庫ロット一覧(数量・賞味期限・購入日)と、この商品名(在庫の全ロットを含む)を削除するボタンを表示する。商品属性・最低数量は表示しない(カード側の⚙️に委ねる)。各ロットには「数量」「期限」の修正ボタン(2026-08-16〜)があり、タップするとそれぞれ4桁ドラムロールピッカー・カレンダーが開いて登録時の入力ミスをその場で直せる(`correctLotQuantity`。詳細はデータモデルの項目を参照)。「購入日・削除」の見出しの下には商品名(`items.name`)+✏️ボタンがあり、タップすると商品名そのものも修正できる(`#pd-item-name-section`、`js/productDetail.js`の`applySectionVisibility`で`pd-lots-section`と同じ条件=item/fallbackモードのみ表示するよう揃えている。2026-08-16、この節を当初masterモードとも共有する`#product-detail-view`内に置いてしまい、CSS詳細度の都合で`.hidden`が効かずmasterモードにも表示されてしまう不具合があったため、item/fallbackモード専用の独立した節に切り出した)
    - **fallbackモード**(商品マスタが無い単独カードの1つだけのボタン): 見出し「商品の詳細」。従来通りすべてをまとめて表示する: 最低数量(`items.low_stock_threshold`)・商品属性(未作成なら「商品属性を作成」ボタン、作成後は読み取り専用+再生成ボタン)・在庫ロット一覧(購入日つき)・削除ボタン。ここで「商品属性を作成」すると、それまで`items.low_stock_threshold`に設定していた値(0より大きい場合のみ)を新しく作った商品マスタに対応する`household_product_settings.low_stock_threshold`へそのまま引き継ぐ(既存の商品マスタを再利用した場合は、他の商品名・他の部屋と共有中の値を上書きしないよう引き継がない)

    共通: 最低数量は在庫確認画面のロット数量と同じ操作感で、`[-][最低数量 単位][+]`で増減でき(個数系の単位は1ずつ、定量系(g/ml/kg/L)は100ずつ)、中央の数量表示をタップすると4桁ドラムロールピッカーが開き直接設定できる(masterモードの単位は、カード内に定量系の商品名が1つでもあればその単位を代表として使う。無ければ個数系の単位を使う、`js/quantity.js`の`representativeUnitForEntries()`)。詳細表示では標準商品名をタップするとひらがな読みが表示される(トグル)。標準商品名は表示名の編集ができなくなったため、以前あった「AIが将来同じ標準商品名と判定した際に既存マスタを再利用するための照合キー(`canonical_normalized_name`)は表示名の編集の影響を受けない」という区別は現在は意味を持たない(表示名自体を編集できないため)。**AI初期値か過去に手動編集された内容かを区別する🤖/👤バッジは廃止済み**(2026-08〜。手動編集自体が廃止済みのため表示する意味が薄れたことと、UIをシンプルにするため)
11. **数量ピッカー**(`#quantity-picker-overlay`): 4桁ドラムロール(0000〜9999の整数)で数量を選択する共通オーバーレイ(`quantityPicker.js`)。手動登録の数量欄・在庫確認のロット数量・消費確認カードの消費数量・商品詳細シートの最低数量から共通で呼び出す。呼び出し元は `openQuantityPicker({ initialValue, unit, title, onConfirm })` を呼ぶだけでよい
12. **購入・消費履歴ページ**(`#view-history`、`js/history.js`): オーバーレイではなく在庫確認画面と同じ独立した`.view`(下部ナビには無いため、左上の丸い戻るボタンでホームへ戻す)。ホーム画面の「購入履歴」カードから開く。日付・種別(食品/日用品)・標準商品名・商品名・区分(購入/消費バッジ)・数量(単位付き)・価格・操作の8列を持つHTML`<table>`で一覧表示する(390px幅には収まらないため`.history-table-wrap`で横スクロールさせる、Excelの表のような一覧)。新しい日付順(`occurred_at`降順)、直近300件まで(`HISTORY_LIMIT`)。タイトル行右端の鉛筆ボタン(`#history-edit-toggle-btn`)で編集モードに切り替えると、通常は非表示の各行の削除ボタン(`.del-btn`)が現れ、各セル(日付・種別・標準商品名・商品名・区分・数量・価格)がその場で手入力できる入力欄/セレクトに変わる(`historyEditRowHtml`。値を変更すると`change`イベントで即座に`item_history`を更新し、表示用キャッシュ`lastHistoryData`にも反映する)。削除は`confirm()`で確認後、その1件だけ`item_history`から削除する(誤って記録された履歴の手動修正用)。**このページは履歴表の閲覧・編集専用**で、サマリー・収支表はここから切り離されており(支出管理メニュー、画面構成13から開く)、表(横スクロール領域)の上には「フィルター」「並び替え」ボタン(いずれも在庫確認画面と同じ`.inventory-filter-btn`)のみを設置する。タイトル行の鉛筆ボタンの隣には「拡大表示」ボタン(`#history-expand-btn`)があり、押すとフルスクリーンの`#history-expand-overlay`を開き、その時点の表示内容(フィルター・並び替え適用済み、常に読み取り専用)を大きく表示する。iPhoneのSafariは画面回転ロックAPI(`screen.orientation.lock`)に対応していないため、端末を回転させなくても横長で見られるよう、CSSで表全体を90度回転させて表示する(`#history-expand-rotator`。端末の縦幅=100vhを回転後の横幅として使う)。ただし端末の自動回転が有効な場合、実際に端末を横向きにするとブラウザのビューポート自体が横長になり、この回転を掛けると二重に回転してしまうため、`@media (orientation: landscape)`でビューポートが既に横長の時は回転を無効化し、そのまま画面いっぱいに表示する(自動回転が無効/ロック中の端末では、ユーザーが手で端末を横向きにすることで正しい向きに見える)。ノッチ・ステータスバー・画面角の丸み(セーフエリア)にタイトル文字や閉じるボタンが隠れないよう、`env(safe-area-inset-*)`(`viewport-fit=cover`指定済みのため実機で実際の値を返す)を余白に加算している。**時計回りに90度回転しているため、回転前のleft/top/right/bottomは実際の画面のtop/right/bottom/leftにそれぞれ1つずつずれて対応する**(回転前の「左」に余白を足すと画面の「上端」=ノッチ・ステータスバー(iPhone14で47pt)から離れる、回転前の「右」に余白を足すと画面の「下端」=ホームインジケーター側から離れる、という対応関係。この対応を誤ると回転していない向きの余白を足すことになり隠れたままになるため注意)。env()が実機で正しく取れない場合に備え、`max()`で固定値(画面上端側は55px、他は16〜28px)を下限にしている。横向きのビューポート(実際に端末側で自動回転した場合)ではこの回転が掛からないため、`@media (orientation: landscape)`内では通常通りtop/right/bottom/leftをそのまま対応させている。タイトルは表示せず表の表示領域を最大化しており、閉じるボタン(`.history-expand-close-btn`)はヘッダー行を持たず表の上に絶対配置で浮かせている(位置は`--expand-pad-top`/`--expand-pad-right`というCSS変数を余白設定とボタン位置の両方で共有し、表のセーフエリア余白と常に一致させる)。表のヘッダー行(sticky)と重なっても埋もれないよう、閉じるボタンは`--primary`色の塗りつぶし+白アイコンにして視認性を確保している。閉じるボタン(✕)でオーバーレイを閉じるだけで、通常表の編集モード・フィルター・並び替えの状態には影響しない。
    - **フィルター**(`#history-filter-overlay`): 在庫確認画面の絞り込み(画面構成5)と同じAmazon風2ペイン構成だが、日付・価格はチェックボックスではなく範囲(from/to)入力にする点が異なる。左側の種類一覧は「日付」(開始日・終了日、`.date-display`から自作カレンダーで選択)・「種別」(食品/日用品、チェックボックス)・「標準商品名」(履歴に登場するものすべてをあ→わ順で列挙、チェックボックス・複数選択可)・「区分」(購入/消費、チェックボックス)・「価格」(最小円・最大円)の5種類(`historyFilterDefs`)。種別/標準商品名/区分どうしのカスケードは無く(在庫確認画面のフィルターとの違い)、すべてAND条件で組み合わさる。右下「結果を表示」で確定、左下「フィルターを解除」はその場でステージをクリアするだけ(オーバーレイは開いたまま)。案内文(`#history-filter-summary`)は選択のたびにリアルタイムで更新される。確定した条件は`js/history.js`がexportする`appliedHistoryFilters`(常に同じオブジェクトを書き換える)と`matchesHistoryFilters(row)`で表現される。**このフィルターは購入履歴ページの表示にのみ適用され、サマリー・収支表(画面構成13〜15)には反映されない**(以前はサマリーと共有していたが、独立させた)
    - **並び替え**(`#history-sort-overlay`): 在庫確認画面の並び替え(2ペイン+階層タブ)のようなUIにはせず、商品登録の「1つ当たり/合計金額」と同じデザインの連結スイッチ(`.price-mode-toggle`、register.cssで定義)を3段並べただけの単純な構成にする。「日付」(新しい順/古い順、常時有効)・「区分」(同じ日付内での並び順。指定なし/消費が上/購入が上)・「金額」(指定なし/高い順/安い順、数量×価格)の3項目を独立して設定でき、日付→区分→金額の優先順位で階層的に並べ替える(区分・金額が両方「指定なし」なら、その日付内は取得時点の順序=`occurred_at`降順のまま。`compareHistoryRows`、Array.sortの安定ソートに依存)。下部は「デフォルトに戻す」(ステージのみ初期値に戻す。日付=新しい順・区分/金額=指定なし)と「並び替え」(確定)の2ボタン。確定した内容は`appliedHistorySort`に保持される。**「ホームに戻る」ボタンを押すと、フィルター(`appliedHistoryFilters`)・並び替え(`appliedHistorySort`)はどちらも初期状態にリセットされる**(次に購入履歴を開いた時は必ずフィルター無し・日付新しい順から始まる。`resetHistoryFiltersAndSort`)
13. **支出管理メニュー**(`#view-expense-menu`、`js/expenseMenu.js`): ホーム画面の「支出管理」カードから開く独立した`.view`(下部ナビには無いため、戻るボタンでホームへ戻す)。集計表(サマリー・収支表)への入口一覧で、`.insight-card`(ホーム画面の「今後追加予定」カードと同じ見た目)を並べた`#expense-menu-list`に「日別カレンダー(購入・消費)」→購入・消費サマリーページ(画面構成14)、「月間集計B/S(貸借対照表)」→在庫収支表ページ(画面構成15)の2枚を表示する。**表の種類を増やせる拡張性を持たせており**、`js/expenseMenu.js`の`EXPENSE_REPORT_DEFS`配列に`{key, label, desc, icon, onOpen}`を1つ追加するだけで新しい集計表への入口を増やせる(`onOpen`には、その集計表を開く処理をまとめて持つモジュール側のexport関数を渡す。例: `js/summary.js`の`openSummaryPage`、`js/balanceSheet.js`の`openBalanceSheetPage`)。ボタンは配列からJSで動的に描画し、クリックは`#expense-menu-list`への委譲1つで拾う
14. **購入・消費サマリーページ**(`#view-summary`、`js/summary.js`): 支出管理メニューの「日別カレンダー」から`openSummaryPage()`で開く独立した`.view`(下部ナビには無いため、戻るボタンで支出管理メニューへ戻す)。日付選択用の自作カレンダー(`js/calendar.js`、画面構成9)とは別モジュールの、集計専用の月間カレンダー。前月/次月ボタンで表示月を切り替え、その月の`item_history`を取得して日付ごとに`quantity×price`を合計する(価格が無い記録は金額を計算できないため集計から除く)。カレンダーのマスは縦長(`.summary-cal-day`)にし、日付の数字を上に詰め、その下に該当する日があれば🛒(緑・購入合計)、➖(オレンジ・消費合計)の2行を表示する(どちらも0円の場合はその行自体を表示しない)。月ラベル(`#summary-cal-month-label`)をタップすると、消費期限入力の自作カレンダー(`#calendar-overlay`、`js/calendar.js`)と同じく、画面の上の層に背景をぼかして表示するオーバーレイ(`#summary-yearmonth-overlay`、`z-index:1100`)で年・月をプルダウン選択できる(ページ内に直置きしない)。年の選択肢は実行時の現在年から+10年まで、月は1〜12月。サマリーは常にどこかの月を表示するため「未設定にする」ボタンは無い。年または月のどちらかを選ぶと消費期限カレンダーと同様即座に確定してオーバーレイが閉じる(背景タップ・下部の「閉じる」ボタンでも閉じる。消費期限カレンダーの`.calendar-footer`をそのまま流用し、「未設定にする」ボタンだけ無い)。今日のマスは`--primary`色の枠で強調する。金額(合計)の計算は`js/history.js`の`computeHistoryRowAmount`を共通で使う(定量系(g/ml/kg/L)は`price`が「100◯当たりの単価」のため`数量÷100×単価`、個数系は`数量×単価`。単純な`数量×単価`だと定量系で100倍の金額になってしまうため、この関数を経由せず独自に計算しないこと)。
    - **フィルター**(`#summary-filter-btn`/`#summary-filter-overlay`): 購入履歴ページの絞り込みと**同じ機能・デザイン**(日付・種別・標準商品名・区分・価格の5種類、Amazon風2ペイン)だが、**購入履歴の`appliedHistoryFilters`とは完全に独立した専用の状態(`appliedSummaryFilters`)を持ち**、このページの集計表示にのみ影響する(購入履歴側のフィルターを変更してもサマリーの表示は変わらず、逆も同様)。標準商品名などの選択肢一覧は、サマリー自体が月ごとの取得しか行わないため、フィルターを開くたびに`item_history`から`canonical_name`/`item_type`/`event_type`のみを軽量に取得し直して列挙する(`loadSummaryFilterSourceRows`)
    - **日付のマスをタップすると、その日の購入履歴を見られる**(`js/history.js`の`openHistoryForDate`。このページのフィルターとは別に、購入履歴側の`appliedHistoryFilters`の日付範囲だけをタップした1日に絞り込んで購入履歴ページへ遷移する、従来通りの動作)。開始日=終了日が同じ1日に絞り込まれている間だけ、購入履歴ページの表は常に同じ値になり冗長な日付列を隠す(`isSingleDayHistoryView`、`.history-table.day-view`。この条件はサマリー経由に限らず、フィルターの日付欄を手動で同じ日にそろえた場合にも同様に働く)。並び順そのものは単一日かどうかによらず、購入履歴ページの「並び替え」(画面構成12)で常時共通の設定に従う
15. **在庫収支表ページ**(`#view-balance`、`js/balanceSheet.js`): 支出管理メニューの「月間集計B/S」から`openBalanceSheetPage()`で開く独立した`.view`(下部ナビには無いため、戻るボタンで支出管理メニューへ戻す)。サマリーページと同じ前月/次月ボタンで表示月を切り替え、月ラベル(`#balance-cal-month-label`)のタップでも同じ操作感の年月プルダウン選択(`#balance-yearmonth-overlay`、背景をぼかしたオーバーレイ、年は実行時の現在年から+10年まで)で切り替えられる。貸借対照表のような2カラムの表で、見出し(`借方(在庫)`/`貸方`)は各半分の中央寄せ、左側「借方(在庫)」に食品/日用品それぞれの「今月末在庫」(自動計算)、右側「貸方」に食品/日用品それぞれの「前月繰越」「今月購入」「今月消費」を表示する。**前月繰越**は表示月の開始日より前の`item_history`全件を、購入は+・消費は-として食品/日用品ごとに累積した金額(=その月の期首在庫評価額。集計期間は購入履歴・サマリーのフィルターとはいずれも独立で、常に月全体)。**今月末在庫**(借方) = 前月繰越+今月購入-今月消費(`js/balanceSheet.js`の`loadBalanceSheet`/`renderBalanceTable`)。この計算式により、借方の総合計(食品+日用品の今月末在庫)と貸方の総合計(食品+日用品それぞれの前月繰越+今月購入-今月消費)は必ず一致する。今月消費は`▲9,000円`のように会計風のマイナス表記にする(`formatAccountingYen`)。金額計算は`js/history.js`の`computeHistoryRowAmount`を共通で使う(サマリーと同じく定量系の単価換算に対応)
16. **ナビバー中央の円形メニュー(登録・消費)**(`#nav-radial-btn`/`#nav-radial-expand`、`js/navRadial.js`): 下部ナビ(`.nav-bar`)は「ホーム・買い物」を左寄せ、「在庫・アカウント」を右寄せにし(`justify-content: space-between`)、上下パディング(`padding-top:8px`/`padding-bottom:calc(8px+env(safe-area-inset-bottom))`)・ボタン幅(62px)・ボタン高さ(44px)・アイコンサイズ(19px)を控えめにして、ラベル下の余白が広くなりすぎないようにしている。左右のパディングは22pxとやや広めにし、「ホーム・買い物」「在庫」の各グループを中央の丸ボタン側へ少し寄せている。中央に空けた隙間から小さいアイコンボタン(`#nav-radial-btn`、直径62px・`bottom: calc(12px + env(safe-area-inset-bottom))`。この`bottom`値は下部ナビの「ホーム」ラベルの下端と揃う高さに合わせている)がはみ出す(PayPayの「支払う」ボタンと同じ考え方。「買い物」アイコンとの間隔が狭くならないよう、直径・ナビバー側のボタン幅ともに控えめにしている)。ボタンには縁取り(白い`border`)を付けず、影も`box-shadow: 0 6px 16px rgba(108,79,246,0.28)`(home.cssの`.home-action-card-icon`と同じ値)程度の控えめなものにして、主張しすぎないようにしている。以前ホーム画面にあった「商品を登録」「商品を消費」のアコーディオンはここに統合された。**アイコンボタンは常時表示され続けるトグルボタン**で、タップするたびに半円メニュー(`#nav-radial-expand`、幅220px・高さ110px・`bottom: calc(61px + env(safe-area-inset-bottom))`。上2つの角だけ半径110pxに丸めて半円形にしている)を開閉する(`open`変数)。開いている間は中身を2段階で差し替える(`state`変数): **choose**(左半分=登録[primary色]/右半分=消費[consume色])→どちらかをタップ→**method**(同じ半円のまま、左半分=AI/右半分=手動に差し替わり、選んだ側(`chosenAction`)の色を全体に反映)→AI/手動をタップすると、登録AIは`#photo-btn`のクリックを起点に`js/aiPhoto.js`が、登録手動は`js/items.js`の`openRegisterManualOverlay()`が、消費AIは`#consume-photo-btn`のクリックを起点に`js/consume.js`が、消費手動は`js/consume.js`の`openConsumeSearchOverlay()`が、それぞれ従来通りの処理を引き継ぎ、半円メニューは閉じる。アイコンボタンの再タップ、または円の外側(`#nav-radial-backdrop`、開いている間だけ表示する透明な全画面レイヤー)のタップでも、どの段階からでも同様に閉じる。左右の半円は、半円形にクリップした`#nav-radial-content`の中に`flex:1`の要素を2つ並べるだけで表現しており、境界線を描画しなくても自然に半円の一部に見える(半円形のクリップは`clip-path: inset(0 round 110px 110px 0 0)`で行う。`border-radius`+`overflow:hidden`の組み合わせは実機のWebKitで角のクリップが効かず白い隙間が見えることがあるため使わない。`#nav-radial-expand`自体の`background`も明示的に`transparent`にしている)。**半円メニューの平らな下端はナビバーの上端にちょうど揃えてあり、ナビバーの半透明な背景と重ならない**(以前は大きな円がナビバーの奥まで重なり、半透明なナビバー越しに透けて見えてしまっていたため、重なり自体を無くす形に変更した)。アイコンボタン(`z-index:960`)・半円メニュー(`z-index:950`)はどちらも外側タップ検知用レイヤー(`z-index:925`)より手前にあるためクリックを奪われず、双方が独立した要素(入れ子でない兄弟要素)のため、以前あったクリックの二重発火やバブリング対策(`stopPropagation`)・クリック判定用の穴あけ(`clip-path`)は不要になった。アイコンボタン・半円メニューの`bottom`にはどちらも`env(safe-area-inset-bottom)`を加算している(ホーム画面に追加してアプリ化した状態では`viewport-fit=cover`が効き、ホームインジケーター分のセーフエリアが下部ナビ(`.nav-bar`、common.css)の下側パディングに丸ごと乗ってバーがその分高くなるため、素の固定pxのままだとアイコンボタンの上端がバーからはみ出さなくなってしまう。ナビバーの高さの変化に追従させることで解消している)

17. **アカウント画面**(`#view-account`、`js/account.js`): 下部ナビの「アカウント」(在庫の右)から開く独立した`.view`。PayPayのアカウント画面を参考にしたレイアウトで、上からルームキー表示(`#account-room-key`、コピーボタン付き。認証画面のキー表示と共通の見た目`.auth-key-display`/`.auth-key-text`を再利用)・「キーを再発行する」リンクボタン(`#account-regenerate-key-btn`)・連絡先メールアドレス表示/変更・参加中メンバー一覧(`#account-member-list`)の順に並べる。キーの再発行は確認ダイアログ(「再発行すると、今のキーでは入室できなくなります」)の後`regenerate_room_key`RPC(`sql/026`)を呼び、成功したら表示中のキーをその場で新しい値に差し替える(参加中メンバーには影響しない。キー漏洩時や、メンバーを削除した相手が古いキーで再入室するのを防ぎたい場合に使う)。連絡先メールアドレス(`households.email`、`#account-household-email`。「キーを忘れた場合」で使う)は読み取り専用で表示し、「メールアドレスを変更する」(`#account-change-email-btn`)を押すと新しいメールアドレスの入力欄(`#account-change-email-form`)が開く。送信すると`signInWithOtp`でマジックリンクを送り(`?auth_intent=change_email&room_key=...`。対象のルームキーはURLのクエリパラメータとして渡す。PWA/Safari間で`localStorage`が共有されないため)、「確認メールを送信しました」の案内(`#account-change-email-sent`。表示したままにしないよう1分後に自動で閉じる)に切り替わる。送信前に今の匿名セッションの認証情報だけを`localStorage`へ一時保存しておき、確認完了後は`js/auth.js`の`handleEmailChangeReturn()`が`update_household_email`RPC(`sql/027`、URLのルームキーだけで完結)で反映する(この時点で変更自体は完了)。その上で、保存しておいた認証情報があれば元のセッションへの復元を試みる(同じ端末・同じブラウザでリンクを開いた場合のみのおまけの利便性向上。詳細はデータモデルの項目を参照)。メンバー一覧は`household_members`を取得してニックネームを列挙し、自分自身の行には「(自分)」ラベルを付ける。**自分の行には削除ボタンの代わりに✏️編集ボタン**(`.account-edit-btn`)があり、タップするとその場でテキスト入力+保存(`.account-edit-save-btn`)/キャンセル(`.account-edit-cancel-btn`)に切り替わる(自分が部屋から抜ける場合はヘッダーの「退室」ボタンを使う。削除ボタンは自分の行には出さない)。保存すると、同じ部屋の他メンバーと同じ名前になっていないかクライアント側でチェックしてから(重複していれば拒否)`household_members.nickname`を`UPDATE`し、ヘッダーの`#user-nickname`表示も更新する(`sql/025`で自分の行のみ更新可能なRLSポリシーを追加済み)。他のメンバーの行には削除ボタン(`.del-btn`)があり、`confirm()`で確認後`household_members`から直接削除する(`sql/023`でDELETEポリシーを追加済み。オーナー/管理者の区別は無く、同じ部屋のメンバーなら誰でも他のメンバーを削除できる)

**注意**: モーダル/シート系のオーバーレイは `z-index: 1000`(`#inventory-filter-overlay`・`#register-overlay`・`#consume-overlay`・`#product-detail-overlay` など)を使うため、`#calendar-overlay`・`#quantity-picker-overlay` は、他のシートの中から開かれても常に手前に表示されるよう `z-index: 1100` にしている。新しいオーバーレイを追加する際、他のシートの中から開く可能性があるならこの点に注意する。ナビバー中央の円形メニュー(`#nav-radial-btn`/`#nav-radial-backdrop`)はナビバー(`z-index:900`)より手前に表示するため`z-index:950〜960`を使う。

下部ナビ(`.nav-bar`)でホーム/買い物/在庫/アカウントを切り替える(`navigation.js`)。ナビバー中央には登録・消費用の円形メニュー(画面構成16)がはみ出して常時表示される。
